{"instruction": ["공항 게이트 보안·수하물 과금 로직을 구현해줘. 여권 만료일, 항공편 상태, 좌석별 수하물 허용량을 검사하고, 규칙은 인터페이스로 분리해서 확장 가능하게 만들어줘.", "SAVE", "요구사항이 바뀌었어. threat_level이 HIGH일 때는 기존 규칙보다 우선해서 전부 SECURITY_CHECK로 보내도록, BaseRule을 상속한 SecurityRule을 추가해줘. GateManager는 기존 전략 패턴을 유지해줘.", "누적 과금 3명 나온 항공편은 4번째 승객부터 허용 무게 -5kg 적용해줘."], "instruction_phase2": ["요구사항이 바뀌었어. threat_level이 HIGH일 때는 기존 규칙보다 우선해서 전부 SECURITY_CHECK로 보내도록, BaseRule을 상속한 SecurityRule을 추가해줘. GateManager는 기존 전략 패턴을 유지해줘.", "누적 과금 3명 나온 항공편은 4번째 승객부터 허용 무게 -5kg 적용해줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": -8.0, "ast_pattern_matched": true, "ast_applicable": true, "has_v1": true, "junior_grade": false, "clarity": 92, "problem_relevance": 95, "rules": 90, "examples": 88, "context": 90}, "label": "A", "analysis_reasoning": "인터페이스·상속·전략 패턴 등 아키텍처 용어를 사용하고, SecurityRule을 rules 최우선 주입으로 확장하여 GateManager.process() 수정 없이 ΔCC를 낮게 유지함. Phase 2 요구사항을 정확히 반영하여 A등급.", "evaluation_log": "[명확성 92] Phase 1·2를 구체적으로 구분하고, 인터페이스·BaseRule 상속·전략 패턴·GateManager 유지 등 설계 용어를 사용함. [문제 적절성 95] 스마트 게이트 2026 요구사항(여권/항공편/수하물, threat_level HIGH→SECURITY_CHECK, 누적 과금 3명→-5kg)과 정확히 일치함. [규칙 90] SecurityRule을 rules 최우선으로 주입하라는 지시가 명확함. [예시·문맥 88·90] 기존 코드 구조를 전제로 한 확장 요청이 분명함. ΔCC -8%, AST 패턴 일치, 최종 A등급."}
{"instruction": ["여권·항공편·수하물 규칙으로 게이트 통과 여부를 판단하는 로직을 만들어줘. 규칙은 인터페이스로 분리하고, GateManager가 전략 패턴으로 규칙을 실행하도록 해줘.", "SAVE", "2차 요구: threat_level HIGH면 모든 승객을 SECURITY_CHECK로 보내는 SecurityRule을 BaseRule 상속으로 추가해줘. GateManager.process()는 수정하지 말고 rules만 주입해서 써줘.", "같은 항공편에서 수하물 과금이 3명 나오면 4번째부터 허용 무게 -5kg 적용해줘."], "instruction_phase2": ["2차 요구: threat_level HIGH면 모든 승객을 SECURITY_CHECK로 보내는 SecurityRule을 BaseRule 상속으로 추가해줘. GateManager.process()는 수정하지 말고 rules만 주입해서 써줘.", "같은 항공편에서 수하물 과금이 3명 나오면 4번째부터 허용 무게 -5kg 적용해줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": -8.0, "ast_pattern_matched": true, "ast_applicable": true, "has_v1": true, "junior_grade": false, "clarity": 92, "problem_relevance": 95, "rules": 90, "examples": 88, "context": 90}, "label": "A", "analysis_reasoning": "인터페이스·상속·전략 패턴 등 아키텍처 용어를 사용하고, SecurityRule을 rules 최우선 주입으로 확장하여 GateManager.process() 수정 없이 ΔCC를 낮게 유지함. Phase 2 요구사항을 정확히 반영하여 A등급.", "evaluation_log": "[명확성 92] Phase 1·2를 구체적으로 구분하고, 인터페이스·BaseRule 상속·전략 패턴·GateManager 유지 등 설계 용어를 사용함. [문제 적절성 95] 스마트 게이트 2026 요구사항(여권/항공편/수하물, threat_level HIGH→SECURITY_CHECK, 누적 과금 3명→-5kg)과 정확히 일치함. [규칙 90] SecurityRule을 rules 최우선으로 주입하라는 지시가 명확함. [예시·문맥 88·90] 기존 코드 구조를 전제로 한 확장 요청이 분명함. ΔCC -8%, AST 패턴 일치, 최종 A등급."}
{"instruction": ["스마트 게이트 로직 구현해줘. 여권 만료, 항공편 상태, 좌석별 수하물 한도를 검사하고, 각 규칙은 인터페이스로 분리해 확장 가능하게 만들어줘.", "SAVE", "변경 요구: threat_level이 HIGH일 때 기존 규칙보다 우선해 SECURITY_CHECK를 반환하는 SecurityRule을 BaseRule 상속해 추가하고, GateManager는 기존 전략 패턴 유지해줘.", "누적 과금 3명 초과한 항공편은 4번째 승객부터 허용 무게 5kg 감소 적용해줘."], "instruction_phase2": ["변경 요구: threat_level이 HIGH일 때 기존 규칙보다 우선해 SECURITY_CHECK를 반환하는 SecurityRule을 BaseRule 상속해 추가하고, GateManager는 기존 전략 패턴 유지해줘.", "누적 과금 3명 초과한 항공편은 4번째 승객부터 허용 무게 5kg 감소 적용해줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": -8.0, "ast_pattern_matched": true, "ast_applicable": true, "has_v1": true, "junior_grade": false, "clarity": 92, "problem_relevance": 95, "rules": 90, "examples": 88, "context": 90}, "label": "A", "analysis_reasoning": "인터페이스·상속·전략 패턴 등 아키텍처 용어를 사용하고, SecurityRule을 rules 최우선 주입으로 확장하여 GateManager.process() 수정 없이 ΔCC를 낮게 유지함. Phase 2 요구사항을 정확히 반영하여 A등급.", "evaluation_log": "[명확성 92] Phase 1·2를 구체적으로 구분하고, 인터페이스·BaseRule 상속·전략 패턴·GateManager 유지 등 설계 용어를 사용함. [문제 적절성 95] 스마트 게이트 2026 요구사항(여권/항공편/수하물, threat_level HIGH→SECURITY_CHECK, 누적 과금 3명→-5kg)과 정확히 일치함. [규칙 90] SecurityRule을 rules 최우선으로 주입하라는 지시가 명확함. [예시·문맥 88·90] 기존 코드 구조를 전제로 한 확장 요청이 분명함. ΔCC -8%, AST 패턴 일치, 최종 A등급."}
{"instruction": ["게이트에서 여권 만료일·항공편 상태·수하물 무게를 검사하는 규칙을 구현해줘. 규칙은 공통 인터페이스로 분리하고, GateManager가 규칙 리스트를 순회하는 전략 패턴으로 동작하게 해줘.", "SAVE", "추가 요구: HIGH 위협 수준일 때 SECURITY_CHECK를 반환하는 SecurityRule을 BaseRule 상속으로 넣고, GateManager의 process() 코드는 건드리지 말고 rules에 SecurityRule을 최우선으로 주입해줘.", "한 항공편에서 과금이 3번 나오면 그 다음 승객부터 허용 무게 -5kg 해줘."], "instruction_phase2": ["추가 요구: HIGH 위협 수준일 때 SECURITY_CHECK를 반환하는 SecurityRule을 BaseRule 상속으로 넣고, GateManager의 process() 코드는 건드리지 말고 rules에 SecurityRule을 최우선으로 주입해줘.", "한 항공편에서 과금이 3번 나오면 그 다음 승객부터 허용 무게 -5kg 해줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": -8.0, "ast_pattern_matched": true, "ast_applicable": true, "has_v1": true, "junior_grade": false, "clarity": 92, "problem_relevance": 95, "rules": 90, "examples": 88, "context": 90}, "label": "A", "analysis_reasoning": "인터페이스·상속·전략 패턴 등 아키텍처 용어를 사용하고, SecurityRule을 rules 최우선 주입으로 확장하여 GateManager.process() 수정 없이 ΔCC를 낮게 유지함. Phase 2 요구사항을 정확히 반영하여 A등급.", "evaluation_log": "[명확성 92] Phase 1·2를 구체적으로 구분하고, 인터페이스·BaseRule 상속·전략 패턴·GateManager 유지 등 설계 용어를 사용함. [문제 적절성 95] 스마트 게이트 2026 요구사항(여권/항공편/수하물, threat_level HIGH→SECURITY_CHECK, 누적 과금 3명→-5kg)과 정확히 일치함. [규칙 90] SecurityRule을 rules 최우선으로 주입하라는 지시가 명확함. [예시·문맥 88·90] 기존 코드 구조를 전제로 한 확장 요청이 분명함. ΔCC -8%, AST 패턴 일치, 최종 A등급."}
{"instruction": ["공항 게이트 보안·수하물 검사 로직을 구현해줘. 여권, 항공편, 수하물 규칙을 각각 클래스로 분리하고 인터페이스로 통일해서, GateManager가 규칙만 바꿔 끼워 넣을 수 있게 해줘.", "SAVE", "요구 변경: threat_level HIGH 시 전부 SECURITY_CHECK 처리하려면 BaseRule을 상속한 SecurityRule을 추가하고, GateManager는 기존처럼 rules 순서만 유지해줘.", "과금 3명 나온 항공편은 4번째부터 허용 무게 5kg 줄여줘."], "instruction_phase2": ["요구 변경: threat_level HIGH 시 전부 SECURITY_CHECK 처리하려면 BaseRule을 상속한 SecurityRule을 추가하고, GateManager는 기존처럼 rules 순서만 유지해줘.", "과금 3명 나온 항공편은 4번째부터 허용 무게 5kg 줄여줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": -8.0, "ast_pattern_matched": true, "ast_applicable": true, "has_v1": true, "junior_grade": false, "clarity": 92, "problem_relevance": 95, "rules": 90, "examples": 88, "context": 90}, "label": "A", "analysis_reasoning": "인터페이스·상속·전략 패턴 등 아키텍처 용어를 사용하고, SecurityRule을 rules 최우선 주입으로 확장하여 GateManager.process() 수정 없이 ΔCC를 낮게 유지함. Phase 2 요구사항을 정확히 반영하여 A등급.", "evaluation_log": "[명확성 92] Phase 1·2를 구체적으로 구분하고, 인터페이스·BaseRule 상속·전략 패턴·GateManager 유지 등 설계 용어를 사용함. [문제 적절성 95] 스마트 게이트 2026 요구사항(여권/항공편/수하물, threat_level HIGH→SECURITY_CHECK, 누적 과금 3명→-5kg)과 정확히 일치함. [규칙 90] SecurityRule을 rules 최우선으로 주입하라는 지시가 명확함. [예시·문맥 88·90] 기존 코드 구조를 전제로 한 확장 요청이 분명함. ΔCC -8%, AST 패턴 일치, 최종 A등급."}
{"instruction": ["여권/항공편/수하물 검사 규칙을 인터페이스 기반으로 분리하고, GateManager가 전략 패턴으로 규칙을 실행하도록 구현해줘.", "SAVE", "2차: threat_level이 HIGH일 때 SECURITY_CHECK를 반환하는 SecurityRule을 BaseRule 상속으로 추가하고, GateManager.process()는 그대로 두고 rules 리스트에 SecurityRule을 맨 앞에 넣어줘.", "동일 항공편에서 수하물 과금이 3명 발생하면 4번째부터 허용 무게 -5kg 적용해줘."], "instruction_phase2": ["2차: threat_level이 HIGH일 때 SECURITY_CHECK를 반환하는 SecurityRule을 BaseRule 상속으로 추가하고, GateManager.process()는 그대로 두고 rules 리스트에 SecurityRule을 맨 앞에 넣어줘.", "동일 항공편에서 수하물 과금이 3명 발생하면 4번째부터 허용 무게 -5kg 적용해줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": -8.0, "ast_pattern_matched": true, "ast_applicable": true, "has_v1": true, "junior_grade": false, "clarity": 92, "problem_relevance": 95, "rules": 90, "examples": 88, "context": 90}, "label": "A", "analysis_reasoning": "인터페이스·상속·전략 패턴 등 아키텍처 용어를 사용하고, SecurityRule을 rules 최우선 주입으로 확장하여 GateManager.process() 수정 없이 ΔCC를 낮게 유지함. Phase 2 요구사항을 정확히 반영하여 A등급.", "evaluation_log": "[명확성 92] Phase 1·2를 구체적으로 구분하고, 인터페이스·BaseRule 상속·전략 패턴·GateManager 유지 등 설계 용어를 사용함. [문제 적절성 95] 스마트 게이트 2026 요구사항(여권/항공편/수하물, threat_level HIGH→SECURITY_CHECK, 누적 과금 3명→-5kg)과 정확히 일치함. [규칙 90] SecurityRule을 rules 최우선으로 주입하라는 지시가 명확함. [예시·문맥 88·90] 기존 코드 구조를 전제로 한 확장 요청이 분명함. ΔCC -8%, AST 패턴 일치, 최종 A등급."}
{"instruction": ["공항 게이트 보안·수하물 과금 로직을 구현해줘. 여권 만료일, 항공편 상태, 좌석별 수하물 허용량을 검사하고, 규칙은 인터페이스로 분리해서 확장 가능하게 만들어줘.", "SAVE", "요구사항이 바뀌었어. threat_level이 HIGH일 때는 기존 규칙보다 우선해서 전부 SECURITY_CHECK로 보내도록, BaseRule을 상속한 SecurityRule을 추가해줘. GateManager는 기존 전략 패턴을 유지해줘.", "누적 과금 3명 나온 항공편은 4번째 승객부터 허용 무게 -5kg 적용해줘."], "instruction_phase2": ["요구사항이 바뀌었어. threat_level이 HIGH일 때는 기존 규칙보다 우선해서 전부 SECURITY_CHECK로 보내도록, BaseRule을 상속한 SecurityRule을 추가해줘. GateManager는 기존 전략 패턴을 유지해줘.", "누적 과금 3명 나온 항공편은 4번째 승객부터 허용 무게 -5kg 적용해줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": -8.0, "ast_pattern_matched": true, "ast_applicable": true, "has_v1": true, "junior_grade": false, "clarity": 92, "problem_relevance": 95, "rules": 90, "examples": 88, "context": 90}, "label": "A", "analysis_reasoning": "인터페이스·상속·전략 패턴 등 아키텍처 용어를 사용하고, SecurityRule을 rules 최우선 주입으로 확장하여 GateManager.process() 수정 없이 ΔCC를 낮게 유지함. Phase 2 요구사항을 정확히 반영하여 A등급.", "evaluation_log": "[명확성 92] Phase 1·2를 구체적으로 구분하고, 인터페이스·BaseRule 상속·전략 패턴·GateManager 유지 등 설계 용어를 사용함. [문제 적절성 95] 스마트 게이트 2026 요구사항(여권/항공편/수하물, threat_level HIGH→SECURITY_CHECK, 누적 과금 3명→-5kg)과 정확히 일치함. [규칙 90] SecurityRule을 rules 최우선으로 주입하라는 지시가 명확함. [예시·문맥 88·90] 기존 코드 구조를 전제로 한 확장 요청이 분명함. ΔCC -8%, AST 패턴 일치, 최종 A등급."}
{"instruction": ["여권·항공편·수하물 규칙으로 게이트 통과 여부를 판단하는 로직을 만들어줘. 규칙은 인터페이스로 분리하고, GateManager가 전략 패턴으로 규칙을 실행하도록 해줘.", "SAVE", "2차 요구: threat_level HIGH면 모든 승객을 SECURITY_CHECK로 보내는 SecurityRule을 BaseRule 상속으로 추가해줘. GateManager.process()는 수정하지 말고 rules만 주입해서 써줘.", "같은 항공편에서 수하물 과금이 3명 나오면 4번째부터 허용 무게 -5kg 적용해줘."], "instruction_phase2": ["2차 요구: threat_level HIGH면 모든 승객을 SECURITY_CHECK로 보내는 SecurityRule을 BaseRule 상속으로 추가해줘. GateManager.process()는 수정하지 말고 rules만 주입해서 써줘.", "같은 항공편에서 수하물 과금이 3명 나오면 4번째부터 허용 무게 -5kg 적용해줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": -8.0, "ast_pattern_matched": true, "ast_applicable": true, "has_v1": true, "junior_grade": false, "clarity": 92, "problem_relevance": 95, "rules": 90, "examples": 88, "context": 90}, "label": "A", "analysis_reasoning": "인터페이스·상속·전략 패턴 등 아키텍처 용어를 사용하고, SecurityRule을 rules 최우선 주입으로 확장하여 GateManager.process() 수정 없이 ΔCC를 낮게 유지함. Phase 2 요구사항을 정확히 반영하여 A등급.", "evaluation_log": "[명확성 92] Phase 1·2를 구체적으로 구분하고, 인터페이스·BaseRule 상속·전략 패턴·GateManager 유지 등 설계 용어를 사용함. [문제 적절성 95] 스마트 게이트 2026 요구사항(여권/항공편/수하물, threat_level HIGH→SECURITY_CHECK, 누적 과금 3명→-5kg)과 정확히 일치함. [규칙 90] SecurityRule을 rules 최우선으로 주입하라는 지시가 명확함. [예시·문맥 88·90] 기존 코드 구조를 전제로 한 확장 요청이 분명함. ΔCC -8%, AST 패턴 일치, 최종 A등급."}
{"instruction": ["스마트 게이트 로직 구현해줘. 여권 만료, 항공편 상태, 좌석별 수하물 한도를 검사하고, 각 규칙은 인터페이스로 분리해 확장 가능하게 만들어줘.", "SAVE", "변경 요구: threat_level이 HIGH일 때 기존 규칙보다 우선해 SECURITY_CHECK를 반환하는 SecurityRule을 BaseRule 상속해 추가하고, GateManager는 기존 전략 패턴 유지해줘.", "누적 과금 3명 초과한 항공편은 4번째 승객부터 허용 무게 5kg 감소 적용해줘."], "instruction_phase2": ["변경 요구: threat_level이 HIGH일 때 기존 규칙보다 우선해 SECURITY_CHECK를 반환하는 SecurityRule을 BaseRule 상속해 추가하고, GateManager는 기존 전략 패턴 유지해줘.", "누적 과금 3명 초과한 항공편은 4번째 승객부터 허용 무게 5kg 감소 적용해줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": -8.0, "ast_pattern_matched": true, "ast_applicable": true, "has_v1": true, "junior_grade": false, "clarity": 92, "problem_relevance": 95, "rules": 90, "examples": 88, "context": 90}, "label": "A", "analysis_reasoning": "인터페이스·상속·전략 패턴 등 아키텍처 용어를 사용하고, SecurityRule을 rules 최우선 주입으로 확장하여 GateManager.process() 수정 없이 ΔCC를 낮게 유지함. Phase 2 요구사항을 정확히 반영하여 A등급.", "evaluation_log": "[명확성 92] Phase 1·2를 구체적으로 구분하고, 인터페이스·BaseRule 상속·전략 패턴·GateManager 유지 등 설계 용어를 사용함. [문제 적절성 95] 스마트 게이트 2026 요구사항(여권/항공편/수하물, threat_level HIGH→SECURITY_CHECK, 누적 과금 3명→-5kg)과 정확히 일치함. [규칙 90] SecurityRule을 rules 최우선으로 주입하라는 지시가 명확함. [예시·문맥 88·90] 기존 코드 구조를 전제로 한 확장 요청이 분명함. ΔCC -8%, AST 패턴 일치, 최종 A등급."}
{"instruction": ["게이트에서 여권 만료일·항공편 상태·수하물 무게를 검사하는 규칙을 구현해줘. 규칙은 공통 인터페이스로 분리하고, GateManager가 규칙 리스트를 순회하는 전략 패턴으로 동작하게 해줘.", "SAVE", "추가 요구: HIGH 위협 수준일 때 SECURITY_CHECK를 반환하는 SecurityRule을 BaseRule 상속으로 넣고, GateManager의 process() 코드는 건드리지 말고 rules에 SecurityRule을 최우선으로 주입해줘.", "한 항공편에서 과금이 3번 나오면 그 다음 승객부터 허용 무게 -5kg 해줘."], "instruction_phase2": ["추가 요구: HIGH 위협 수준일 때 SECURITY_CHECK를 반환하는 SecurityRule을 BaseRule 상속으로 넣고, GateManager의 process() 코드는 건드리지 말고 rules에 SecurityRule을 최우선으로 주입해줘.", "한 항공편에서 과금이 3번 나오면 그 다음 승객부터 허용 무게 -5kg 해줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": -8.0, "ast_pattern_matched": true, "ast_applicable": true, "has_v1": true, "junior_grade": false, "clarity": 92, "problem_relevance": 95, "rules": 90, "examples": 88, "context": 90}, "label": "A", "analysis_reasoning": "인터페이스·상속·전략 패턴 등 아키텍처 용어를 사용하고, SecurityRule을 rules 최우선 주입으로 확장하여 GateManager.process() 수정 없이 ΔCC를 낮게 유지함. Phase 2 요구사항을 정확히 반영하여 A등급.", "evaluation_log": "[명확성 92] Phase 1·2를 구체적으로 구분하고, 인터페이스·BaseRule 상속·전략 패턴·GateManager 유지 등 설계 용어를 사용함. [문제 적절성 95] 스마트 게이트 2026 요구사항(여권/항공편/수하물, threat_level HIGH→SECURITY_CHECK, 누적 과금 3명→-5kg)과 정확히 일치함. [규칙 90] SecurityRule을 rules 최우선으로 주입하라는 지시가 명확함. [예시·문맥 88·90] 기존 코드 구조를 전제로 한 확장 요청이 분명함. ΔCC -8%, AST 패턴 일치, 최종 A등급."}
{"instruction": ["공항 게이트 보안·수하물 검사 로직을 구현해줘. 여권, 항공편, 수하물 규칙을 각각 클래스로 분리하고 인터페이스로 통일해서, GateManager가 규칙만 바꿔 끼워 넣을 수 있게 해줘.", "SAVE", "요구 변경: threat_level HIGH 시 전부 SECURITY_CHECK 처리하려면 BaseRule을 상속한 SecurityRule을 추가하고, GateManager는 기존처럼 rules 순서만 유지해줘.", "과금 3명 나온 항공편은 4번째부터 허용 무게 5kg 줄여줘."], "instruction_phase2": ["요구 변경: threat_level HIGH 시 전부 SECURITY_CHECK 처리하려면 BaseRule을 상속한 SecurityRule을 추가하고, GateManager는 기존처럼 rules 순서만 유지해줘.", "과금 3명 나온 항공편은 4번째부터 허용 무게 5kg 줄여줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": -8.0, "ast_pattern_matched": true, "ast_applicable": true, "has_v1": true, "junior_grade": false, "clarity": 92, "problem_relevance": 95, "rules": 90, "examples": 88, "context": 90}, "label": "A", "analysis_reasoning": "인터페이스·상속·전략 패턴 등 아키텍처 용어를 사용하고, SecurityRule을 rules 최우선 주입으로 확장하여 GateManager.process() 수정 없이 ΔCC를 낮게 유지함. Phase 2 요구사항을 정확히 반영하여 A등급.", "evaluation_log": "[명확성 92] Phase 1·2를 구체적으로 구분하고, 인터페이스·BaseRule 상속·전략 패턴·GateManager 유지 등 설계 용어를 사용함. [문제 적절성 95] 스마트 게이트 2026 요구사항(여권/항공편/수하물, threat_level HIGH→SECURITY_CHECK, 누적 과금 3명→-5kg)과 정확히 일치함. [규칙 90] SecurityRule을 rules 최우선으로 주입하라는 지시가 명확함. [예시·문맥 88·90] 기존 코드 구조를 전제로 한 확장 요청이 분명함. ΔCC -8%, AST 패턴 일치, 최종 A등급."}
{"instruction": ["여권/항공편/수하물 검사 규칙을 인터페이스 기반으로 분리하고, GateManager가 전략 패턴으로 규칙을 실행하도록 구현해줘.", "SAVE", "2차: threat_level이 HIGH일 때 SECURITY_CHECK를 반환하는 SecurityRule을 BaseRule 상속으로 추가하고, GateManager.process()는 그대로 두고 rules 리스트에 SecurityRule을 맨 앞에 넣어줘.", "동일 항공편에서 수하물 과금이 3명 발생하면 4번째부터 허용 무게 -5kg 적용해줘."], "instruction_phase2": ["2차: threat_level이 HIGH일 때 SECURITY_CHECK를 반환하는 SecurityRule을 BaseRule 상속으로 추가하고, GateManager.process()는 그대로 두고 rules 리스트에 SecurityRule을 맨 앞에 넣어줘.", "동일 항공편에서 수하물 과금이 3명 발생하면 4번째부터 허용 무게 -5kg 적용해줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": -8.0, "ast_pattern_matched": true, "ast_applicable": true, "has_v1": true, "junior_grade": false, "clarity": 92, "problem_relevance": 95, "rules": 90, "examples": 88, "context": 90}, "label": "A", "analysis_reasoning": "인터페이스·상속·전략 패턴 등 아키텍처 용어를 사용하고, SecurityRule을 rules 최우선 주입으로 확장하여 GateManager.process() 수정 없이 ΔCC를 낮게 유지함. Phase 2 요구사항을 정확히 반영하여 A등급.", "evaluation_log": "[명확성 92] Phase 1·2를 구체적으로 구분하고, 인터페이스·BaseRule 상속·전략 패턴·GateManager 유지 등 설계 용어를 사용함. [문제 적절성 95] 스마트 게이트 2026 요구사항(여권/항공편/수하물, threat_level HIGH→SECURITY_CHECK, 누적 과금 3명→-5kg)과 정확히 일치함. [규칙 90] SecurityRule을 rules 최우선으로 주입하라는 지시가 명확함. [예시·문맥 88·90] 기존 코드 구조를 전제로 한 확장 요청이 분명함. ΔCC -8%, AST 패턴 일치, 최종 A등급."}
{"instruction": ["게이트에서 여권 만료, 항공편 상태, 수하물 무게 검사하는 거 구현해줘. 규칙은 나눠서 넣을 수 있게 해줘.", "SAVE", "threat_level이 HIGH면 모든 승객 SECURITY_CHECK로 보내게 해줘. BaseRule 상속하는 SecurityRule 추가하고, GateManager는 기존처럼 rules 순서대로만 돌려줘.", "같은 항공편에서 과금 3명 나오면 그 다음부터 허용 무게 5kg 깎아줘."], "instruction_phase2": ["threat_level이 HIGH면 모든 승객 SECURITY_CHECK로 보내게 해줘. BaseRule 상속하는 SecurityRule 추가하고, GateManager는 기존처럼 rules 순서대로만 돌려줘.", "같은 항공편에서 과금 3명 나오면 그 다음부터 허용 무게 5kg 깎아줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 25.0, "ast_pattern_matched": true, "ast_applicable": true, "has_v1": true, "junior_grade": false, "clarity": 82, "problem_relevance": 85, "rules": 75, "examples": 70, "context": 80}, "label": "B", "analysis_reasoning": "이 프롬프트는 아키텍처 용어를 구체적으로 사용하지 않아 설계 지침이 부족함(B등급 사유). 기능 요구는 전달되나 규칙·예시·문맥 명확성이 A보다 낮음.", "evaluation_log": "[명확성 82] 규칙을 나눠서 넣을 수 있게 해달라는 수준으로, 인터페이스·상속 용어는 없음. 설계 지침이 부족함. [문제 적절성 85] SECURITY_CHECK·과금 3명 후 -5kg 등 요구는 전달되나 구체성은 A보다 낮음. [규칙 75] BaseRule 상속 SecurityRule 추가는 언급되나 GateManager 전략 패턴 유지는 없음. B등급."}
{"instruction": ["게이트 검사 로직 구현해줘. 여권 만료, 항공편, 수하물 무게 검사하고, 규칙은 따로 클래스로 나눠서 확장 가능하게 해줘.", "SAVE", "HIGH일 때 전원 SECURITY_CHECK 보내게 SecurityRule 추가해줘. BaseRule 상속하고, GateManager는 rules 그대로 순서만 돌려줘.", "과금 3명 나온 항공편은 그 다음부터 허용 5kg 깎아줘."], "instruction_phase2": ["HIGH일 때 전원 SECURITY_CHECK 보내게 SecurityRule 추가해줘. BaseRule 상속하고, GateManager는 rules 그대로 순서만 돌려줘.", "과금 3명 나온 항공편은 그 다음부터 허용 5kg 깎아줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 25.0, "ast_pattern_matched": true, "ast_applicable": true, "has_v1": true, "junior_grade": false, "clarity": 82, "problem_relevance": 85, "rules": 75, "examples": 70, "context": 80}, "label": "B", "analysis_reasoning": "이 프롬프트는 아키텍처 용어를 구체적으로 사용하지 않아 설계 지침이 부족함(B등급 사유). 기능 요구는 전달되나 규칙·예시·문맥 명확성이 A보다 낮음.", "evaluation_log": "[명확성 82] 규칙을 나눠서 넣을 수 있게 해달라는 수준으로, 인터페이스·상속 용어는 없음. 설계 지침이 부족함. [문제 적절성 85] SECURITY_CHECK·과금 3명 후 -5kg 등 요구는 전달되나 구체성은 A보다 낮음. [규칙 75] BaseRule 상속 SecurityRule 추가는 언급되나 GateManager 전략 패턴 유지는 없음. B등급."}
{"instruction": ["공항 게이트에서 여권/항공편/수하물 검사하는 거 만들어줘. 규칙은 나눠서 넣을 수 있게만 해줘.", "SAVE", "threat_level HIGH면 다 SECURITY_CHECK로 보내게 해줘. SecurityRule 추가하고 BaseRule 상속, GateManager는 기존처럼 rules만 돌려줘.", "같은 편에서 3명 과금 나오면 다음부터 허용 무게 5kg 줄여줘."], "instruction_phase2": ["threat_level HIGH면 다 SECURITY_CHECK로 보내게 해줘. SecurityRule 추가하고 BaseRule 상속, GateManager는 기존처럼 rules만 돌려줘.", "같은 편에서 3명 과금 나오면 다음부터 허용 무게 5kg 줄여줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 25.0, "ast_pattern_matched": true, "ast_applicable": true, "has_v1": true, "junior_grade": false, "clarity": 82, "problem_relevance": 85, "rules": 75, "examples": 70, "context": 80}, "label": "B", "analysis_reasoning": "이 프롬프트는 아키텍처 용어를 구체적으로 사용하지 않아 설계 지침이 부족함(B등급 사유). 기능 요구는 전달되나 규칙·예시·문맥 명확성이 A보다 낮음.", "evaluation_log": "[명확성 82] 규칙을 나눠서 넣을 수 있게 해달라는 수준으로, 인터페이스·상속 용어는 없음. 설계 지침이 부족함. [문제 적절성 85] SECURITY_CHECK·과금 3명 후 -5kg 등 요구는 전달되나 구체성은 A보다 낮음. [규칙 75] BaseRule 상속 SecurityRule 추가는 언급되나 GateManager 전략 패턴 유지는 없음. B등급."}
{"instruction": ["여권·항공편·수하물 검사하는 게이트 로직 구현해줘. 규칙은 분리해서 넣을 수 있게 해줘.", "SAVE", "위협 수준 HIGH면 전부 SECURITY_CHECK로 보내도록 SecurityRule을 BaseRule 상속해서 추가해줘. GateManager는 rules 순서대로만 실행해줘.", "한 항공편에서 과금 3번 나오면 그다음부터 허용 무게 5kg 감소해줘."], "instruction_phase2": ["위협 수준 HIGH면 전부 SECURITY_CHECK로 보내도록 SecurityRule을 BaseRule 상속해서 추가해줘. GateManager는 rules 순서대로만 실행해줘.", "한 항공편에서 과금 3번 나오면 그다음부터 허용 무게 5kg 감소해줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 25.0, "ast_pattern_matched": true, "ast_applicable": true, "has_v1": true, "junior_grade": false, "clarity": 82, "problem_relevance": 85, "rules": 75, "examples": 70, "context": 80}, "label": "B", "analysis_reasoning": "이 프롬프트는 아키텍처 용어를 구체적으로 사용하지 않아 설계 지침이 부족함(B등급 사유). 기능 요구는 전달되나 규칙·예시·문맥 명확성이 A보다 낮음.", "evaluation_log": "[명확성 82] 규칙을 나눠서 넣을 수 있게 해달라는 수준으로, 인터페이스·상속 용어는 없음. 설계 지침이 부족함. [문제 적절성 85] SECURITY_CHECK·과금 3명 후 -5kg 등 요구는 전달되나 구체성은 A보다 낮음. [규칙 75] BaseRule 상속 SecurityRule 추가는 언급되나 GateManager 전략 패턴 유지는 없음. B등급."}
{"instruction": ["게이트에서 여권 만료, 항공편 상태, 수하물 검사 구현해줘. 규칙은 나눠서 조합할 수 있게 해줘.", "SAVE", "HIGH면 모두 SECURITY_CHECK 처리하는 SecurityRule 추가해줘. BaseRule 상속, GateManager는 기존 rules 순서 유지해줘.", "3명 과금 나온 항공편은 4번째부터 허용 5kg 깎아줘."], "instruction_phase2": ["HIGH면 모두 SECURITY_CHECK 처리하는 SecurityRule 추가해줘. BaseRule 상속, GateManager는 기존 rules 순서 유지해줘.", "3명 과금 나온 항공편은 4번째부터 허용 5kg 깎아줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 25.0, "ast_pattern_matched": true, "ast_applicable": true, "has_v1": true, "junior_grade": false, "clarity": 82, "problem_relevance": 85, "rules": 75, "examples": 70, "context": 80}, "label": "B", "analysis_reasoning": "이 프롬프트는 아키텍처 용어를 구체적으로 사용하지 않아 설계 지침이 부족함(B등급 사유). 기능 요구는 전달되나 규칙·예시·문맥 명확성이 A보다 낮음.", "evaluation_log": "[명확성 82] 규칙을 나눠서 넣을 수 있게 해달라는 수준으로, 인터페이스·상속 용어는 없음. 설계 지침이 부족함. [문제 적절성 85] SECURITY_CHECK·과금 3명 후 -5kg 등 요구는 전달되나 구체성은 A보다 낮음. [규칙 75] BaseRule 상속 SecurityRule 추가는 언급되나 GateManager 전략 패턴 유지는 없음. B등급."}
{"instruction": ["공항 게이트 여권/항공편/수하물 검사 로직 만들어줘. 규칙은 분리해서 확장 가능하게 해줘.", "SAVE", "threat_level HIGH일 때 전원 SECURITY_CHECK로 보내게 SecurityRule(BaseRule 상속) 추가하고, GateManager는 rules만 순서대로 돌려줘.", "같은 항공편 과금 3명이면 다음부터 허용 무게 -5kg 해줘."], "instruction_phase2": ["threat_level HIGH일 때 전원 SECURITY_CHECK로 보내게 SecurityRule(BaseRule 상속) 추가하고, GateManager는 rules만 순서대로 돌려줘.", "같은 항공편 과금 3명이면 다음부터 허용 무게 -5kg 해줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 25.0, "ast_pattern_matched": true, "ast_applicable": true, "has_v1": true, "junior_grade": false, "clarity": 82, "problem_relevance": 85, "rules": 75, "examples": 70, "context": 80}, "label": "B", "analysis_reasoning": "이 프롬프트는 아키텍처 용어를 구체적으로 사용하지 않아 설계 지침이 부족함(B등급 사유). 기능 요구는 전달되나 규칙·예시·문맥 명확성이 A보다 낮음.", "evaluation_log": "[명확성 82] 규칙을 나눠서 넣을 수 있게 해달라는 수준으로, 인터페이스·상속 용어는 없음. 설계 지침이 부족함. [문제 적절성 85] SECURITY_CHECK·과금 3명 후 -5kg 등 요구는 전달되나 구체성은 A보다 낮음. [규칙 75] BaseRule 상속 SecurityRule 추가는 언급되나 GateManager 전략 패턴 유지는 없음. B등급."}
{"instruction": ["게이트에서 여권 만료, 항공편 상태, 수하물 무게 검사하는 거 구현해줘. 규칙은 나눠서 넣을 수 있게 해줘.", "SAVE", "threat_level이 HIGH면 모든 승객 SECURITY_CHECK로 보내게 해줘. BaseRule 상속하는 SecurityRule 추가하고, GateManager는 기존처럼 rules 순서대로만 돌려줘.", "같은 항공편에서 과금 3명 나오면 그 다음부터 허용 무게 5kg 깎아줘."], "instruction_phase2": ["threat_level이 HIGH면 모든 승객 SECURITY_CHECK로 보내게 해줘. BaseRule 상속하는 SecurityRule 추가하고, GateManager는 기존처럼 rules 순서대로만 돌려줘.", "같은 항공편에서 과금 3명 나오면 그 다음부터 허용 무게 5kg 깎아줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 25.0, "ast_pattern_matched": true, "ast_applicable": true, "has_v1": true, "junior_grade": false, "clarity": 82, "problem_relevance": 85, "rules": 75, "examples": 70, "context": 80}, "label": "B", "analysis_reasoning": "이 프롬프트는 아키텍처 용어를 구체적으로 사용하지 않아 설계 지침이 부족함(B등급 사유). 기능 요구는 전달되나 규칙·예시·문맥 명확성이 A보다 낮음.", "evaluation_log": "[명확성 82] 규칙을 나눠서 넣을 수 있게 해달라는 수준으로, 인터페이스·상속 용어는 없음. 설계 지침이 부족함. [문제 적절성 85] SECURITY_CHECK·과금 3명 후 -5kg 등 요구는 전달되나 구체성은 A보다 낮음. [규칙 75] BaseRule 상속 SecurityRule 추가는 언급되나 GateManager 전략 패턴 유지는 없음. B등급."}
{"instruction": ["게이트 검사 로직 구현해줘. 여권 만료, 항공편, 수하물 무게 검사하고, 규칙은 따로 클래스로 나눠서 확장 가능하게 해줘.", "SAVE", "HIGH일 때 전원 SECURITY_CHECK 보내게 SecurityRule 추가해줘. BaseRule 상속하고, GateManager는 rules 그대로 순서만 돌려줘.", "과금 3명 나온 항공편은 그 다음부터 허용 5kg 깎아줘."], "instruction_phase2": ["HIGH일 때 전원 SECURITY_CHECK 보내게 SecurityRule 추가해줘. BaseRule 상속하고, GateManager는 rules 그대로 순서만 돌려줘.", "과금 3명 나온 항공편은 그 다음부터 허용 5kg 깎아줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 25.0, "ast_pattern_matched": true, "ast_applicable": true, "has_v1": true, "junior_grade": false, "clarity": 82, "problem_relevance": 85, "rules": 75, "examples": 70, "context": 80}, "label": "B", "analysis_reasoning": "이 프롬프트는 아키텍처 용어를 구체적으로 사용하지 않아 설계 지침이 부족함(B등급 사유). 기능 요구는 전달되나 규칙·예시·문맥 명확성이 A보다 낮음.", "evaluation_log": "[명확성 82] 규칙을 나눠서 넣을 수 있게 해달라는 수준으로, 인터페이스·상속 용어는 없음. 설계 지침이 부족함. [문제 적절성 85] SECURITY_CHECK·과금 3명 후 -5kg 등 요구는 전달되나 구체성은 A보다 낮음. [규칙 75] BaseRule 상속 SecurityRule 추가는 언급되나 GateManager 전략 패턴 유지는 없음. B등급."}
{"instruction": ["공항 게이트에서 여권/항공편/수하물 검사하는 거 만들어줘. 규칙은 나눠서 넣을 수 있게만 해줘.", "SAVE", "threat_level HIGH면 다 SECURITY_CHECK로 보내게 해줘. SecurityRule 추가하고 BaseRule 상속, GateManager는 기존처럼 rules만 돌려줘.", "같은 편에서 3명 과금 나오면 다음부터 허용 무게 5kg 줄여줘."], "instruction_phase2": ["threat_level HIGH면 다 SECURITY_CHECK로 보내게 해줘. SecurityRule 추가하고 BaseRule 상속, GateManager는 기존처럼 rules만 돌려줘.", "같은 편에서 3명 과금 나오면 다음부터 허용 무게 5kg 줄여줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 25.0, "ast_pattern_matched": true, "ast_applicable": true, "has_v1": true, "junior_grade": false, "clarity": 82, "problem_relevance": 85, "rules": 75, "examples": 70, "context": 80}, "label": "B", "analysis_reasoning": "이 프롬프트는 아키텍처 용어를 구체적으로 사용하지 않아 설계 지침이 부족함(B등급 사유). 기능 요구는 전달되나 규칙·예시·문맥 명확성이 A보다 낮음.", "evaluation_log": "[명확성 82] 규칙을 나눠서 넣을 수 있게 해달라는 수준으로, 인터페이스·상속 용어는 없음. 설계 지침이 부족함. [문제 적절성 85] SECURITY_CHECK·과금 3명 후 -5kg 등 요구는 전달되나 구체성은 A보다 낮음. [규칙 75] BaseRule 상속 SecurityRule 추가는 언급되나 GateManager 전략 패턴 유지는 없음. B등급."}
{"instruction": ["여권·항공편·수하물 검사하는 게이트 로직 구현해줘. 규칙은 분리해서 넣을 수 있게 해줘.", "SAVE", "위협 수준 HIGH면 전부 SECURITY_CHECK로 보내도록 SecurityRule을 BaseRule 상속해서 추가해줘. GateManager는 rules 순서대로만 실행해줘.", "한 항공편에서 과금 3번 나오면 그다음부터 허용 무게 5kg 감소해줘."], "instruction_phase2": ["위협 수준 HIGH면 전부 SECURITY_CHECK로 보내도록 SecurityRule을 BaseRule 상속해서 추가해줘. GateManager는 rules 순서대로만 실행해줘.", "한 항공편에서 과금 3번 나오면 그다음부터 허용 무게 5kg 감소해줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 25.0, "ast_pattern_matched": true, "ast_applicable": true, "has_v1": true, "junior_grade": false, "clarity": 82, "problem_relevance": 85, "rules": 75, "examples": 70, "context": 80}, "label": "B", "analysis_reasoning": "이 프롬프트는 아키텍처 용어를 구체적으로 사용하지 않아 설계 지침이 부족함(B등급 사유). 기능 요구는 전달되나 규칙·예시·문맥 명확성이 A보다 낮음.", "evaluation_log": "[명확성 82] 규칙을 나눠서 넣을 수 있게 해달라는 수준으로, 인터페이스·상속 용어는 없음. 설계 지침이 부족함. [문제 적절성 85] SECURITY_CHECK·과금 3명 후 -5kg 등 요구는 전달되나 구체성은 A보다 낮음. [규칙 75] BaseRule 상속 SecurityRule 추가는 언급되나 GateManager 전략 패턴 유지는 없음. B등급."}
{"instruction": ["게이트에서 여권 만료, 항공편 상태, 수하물 검사 구현해줘. 규칙은 나눠서 조합할 수 있게 해줘.", "SAVE", "HIGH면 모두 SECURITY_CHECK 처리하는 SecurityRule 추가해줘. BaseRule 상속, GateManager는 기존 rules 순서 유지해줘.", "3명 과금 나온 항공편은 4번째부터 허용 5kg 깎아줘."], "instruction_phase2": ["HIGH면 모두 SECURITY_CHECK 처리하는 SecurityRule 추가해줘. BaseRule 상속, GateManager는 기존 rules 순서 유지해줘.", "3명 과금 나온 항공편은 4번째부터 허용 5kg 깎아줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 25.0, "ast_pattern_matched": true, "ast_applicable": true, "has_v1": true, "junior_grade": false, "clarity": 82, "problem_relevance": 85, "rules": 75, "examples": 70, "context": 80}, "label": "B", "analysis_reasoning": "이 프롬프트는 아키텍처 용어를 구체적으로 사용하지 않아 설계 지침이 부족함(B등급 사유). 기능 요구는 전달되나 규칙·예시·문맥 명확성이 A보다 낮음.", "evaluation_log": "[명확성 82] 규칙을 나눠서 넣을 수 있게 해달라는 수준으로, 인터페이스·상속 용어는 없음. 설계 지침이 부족함. [문제 적절성 85] SECURITY_CHECK·과금 3명 후 -5kg 등 요구는 전달되나 구체성은 A보다 낮음. [규칙 75] BaseRule 상속 SecurityRule 추가는 언급되나 GateManager 전략 패턴 유지는 없음. B등급."}
{"instruction": ["공항 게이트 여권/항공편/수하물 검사 로직 만들어줘. 규칙은 분리해서 확장 가능하게 해줘.", "SAVE", "threat_level HIGH일 때 전원 SECURITY_CHECK로 보내게 SecurityRule(BaseRule 상속) 추가하고, GateManager는 rules만 순서대로 돌려줘.", "같은 항공편 과금 3명이면 다음부터 허용 무게 -5kg 해줘."], "instruction_phase2": ["threat_level HIGH일 때 전원 SECURITY_CHECK로 보내게 SecurityRule(BaseRule 상속) 추가하고, GateManager는 rules만 순서대로 돌려줘.", "같은 항공편 과금 3명이면 다음부터 허용 무게 -5kg 해줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 25.0, "ast_pattern_matched": true, "ast_applicable": true, "has_v1": true, "junior_grade": false, "clarity": 82, "problem_relevance": 85, "rules": 75, "examples": 70, "context": 80}, "label": "B", "analysis_reasoning": "이 프롬프트는 아키텍처 용어를 구체적으로 사용하지 않아 설계 지침이 부족함(B등급 사유). 기능 요구는 전달되나 규칙·예시·문맥 명확성이 A보다 낮음.", "evaluation_log": "[명확성 82] 규칙을 나눠서 넣을 수 있게 해달라는 수준으로, 인터페이스·상속 용어는 없음. 설계 지침이 부족함. [문제 적절성 85] SECURITY_CHECK·과금 3명 후 -5kg 등 요구는 전달되나 구체성은 A보다 낮음. [규칙 75] BaseRule 상속 SecurityRule 추가는 언급되나 GateManager 전략 패턴 유지는 없음. B등급."}
{"instruction": ["공항 게이트 로직 만들어줘. 여권이랑 항공편 상태, 수하물 보면서 통과/거절 하는 거.", "SAVE", "보안이 HIGH일 때는 다 SECURITY_CHECK로 보내주고, 과금 3번 나온 편은 4번째 사람부터 5kg 빼서 적용해줘.", "기존 코드 구조 유지하면서만 넣어줘."], "instruction_phase2": ["보안이 HIGH일 때는 다 SECURITY_CHECK로 보내주고, 과금 3번 나온 편은 4번째 사람부터 5kg 빼서 적용해줘.", "기존 코드 구조 유지하면서만 넣어줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 45.0, "ast_pattern_matched": false, "ast_applicable": true, "has_v1": true, "junior_grade": true, "clarity": 58, "problem_relevance": 55, "rules": 50, "examples": 48, "context": 52}, "label": "C", "analysis_reasoning": "주니어 톤으로 기능만 나열하여 요청함. 코드가 비대해지거나 avg_cc 상승·기존 구조 유지가 약함. 명확성·문제 적절성·규칙 반영이 50점대로 C등급.", "evaluation_log": "[명확성 58] 보안 HIGH, 과금 3번 나온 편 등 비형식적 표현으로 요청함. [문제 적절성 55] Phase 2 요구는 있으나 기존 구조 유지만 언급되고 설계 수준 지시 없음. [규칙 50] 코드 구조 유지하면서만 넣어달라로만 되어 있어 규칙·예시 점수 50점대. 주니어 톤, ΔCC 상승·구조 붕괴 위험으로 C등급."}
{"instruction": ["게이트 로직 만들어줘. 여권이랑 항공편, 수하물 보고 통과/거절 해줘.", "SAVE", "보안 HIGH면 다 SECURITY_CHECK로 보내고, 과금 3번 나온 편은 4번째부터 5kg 빼줘. 구조 유지하면서만 넣어줘."], "instruction_phase2": ["보안 HIGH면 다 SECURITY_CHECK로 보내고, 과금 3번 나온 편은 4번째부터 5kg 빼줘. 구조 유지하면서만 넣어줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 45.0, "ast_pattern_matched": false, "ast_applicable": true, "has_v1": true, "junior_grade": true, "clarity": 58, "problem_relevance": 55, "rules": 50, "examples": 48, "context": 52}, "label": "C", "analysis_reasoning": "주니어 톤으로 기능만 나열하여 요청함. 코드가 비대해지거나 avg_cc 상승·기존 구조 유지가 약함. 명확성·문제 적절성·규칙 반영이 50점대로 C등급.", "evaluation_log": "[명확성 58] 보안 HIGH, 과금 3번 나온 편 등 비형식적 표현으로 요청함. [문제 적절성 55] Phase 2 요구는 있으나 기존 구조 유지만 언급되고 설계 수준 지시 없음. [규칙 50] 코드 구조 유지하면서만 넣어달라로만 되어 있어 규칙·예시 점수 50점대. 주니어 톤, ΔCC 상승·구조 붕괴 위험으로 C등급."}
{"instruction": ["공항 게이트에서 여권, 항공편, 수하물 검사하는 거 구현해줘.", "SAVE", "위험도 HIGH일 때 전부 SECURITY_CHECK로 보내주고, 3명 과금 나온 편은 4번째 사람부터 5kg 빼서 적용해줘. 기존 구조 유지해줘."], "instruction_phase2": ["위험도 HIGH일 때 전부 SECURITY_CHECK로 보내주고, 3명 과금 나온 편은 4번째 사람부터 5kg 빼서 적용해줘. 기존 구조 유지해줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 45.0, "ast_pattern_matched": false, "ast_applicable": true, "has_v1": true, "junior_grade": true, "clarity": 58, "problem_relevance": 55, "rules": 50, "examples": 48, "context": 52}, "label": "C", "analysis_reasoning": "주니어 톤으로 기능만 나열하여 요청함. 코드가 비대해지거나 avg_cc 상승·기존 구조 유지가 약함. 명확성·문제 적절성·규칙 반영이 50점대로 C등급.", "evaluation_log": "[명확성 58] 보안 HIGH, 과금 3번 나온 편 등 비형식적 표현으로 요청함. [문제 적절성 55] Phase 2 요구는 있으나 기존 구조 유지만 언급되고 설계 수준 지시 없음. [규칙 50] 코드 구조 유지하면서만 넣어달라로만 되어 있어 규칙·예시 점수 50점대. 주니어 톤, ΔCC 상승·구조 붕괴 위험으로 C등급."}
{"instruction": ["여권·항공편·수하물 검사하는 게이트 코드 만들어줘.", "SAVE", "보안이 높을 때는 SECURITY_CHECK로 보내고, 과금 많이 나온 편은 4번째부터 5kg 줄여줘. 기존 코드 구조 유지하면서 넣어줘."], "instruction_phase2": ["보안이 높을 때는 SECURITY_CHECK로 보내고, 과금 많이 나온 편은 4번째부터 5kg 줄여줘. 기존 코드 구조 유지하면서 넣어줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 45.0, "ast_pattern_matched": false, "ast_applicable": true, "has_v1": true, "junior_grade": true, "clarity": 58, "problem_relevance": 55, "rules": 50, "examples": 48, "context": 52}, "label": "C", "analysis_reasoning": "주니어 톤으로 기능만 나열하여 요청함. 코드가 비대해지거나 avg_cc 상승·기존 구조 유지가 약함. 명확성·문제 적절성·규칙 반영이 50점대로 C등급.", "evaluation_log": "[명확성 58] 보안 HIGH, 과금 3번 나온 편 등 비형식적 표현으로 요청함. [문제 적절성 55] Phase 2 요구는 있으나 기존 구조 유지만 언급되고 설계 수준 지시 없음. [규칙 50] 코드 구조 유지하면서만 넣어달라로만 되어 있어 규칙·예시 점수 50점대. 주니어 톤, ΔCC 상승·구조 붕괴 위험으로 C등급."}
{"instruction": ["게이트 통과/거절 로직 만들어줘. 여권, 항공편 상태, 수하물 봐줘.", "SAVE", "HIGH일 때 다 SECURITY_CHECK, 과금 3번 나온 편은 그 다음부터 5kg 빼줘. 구조만 유지해서 넣어줘."], "instruction_phase2": ["HIGH일 때 다 SECURITY_CHECK, 과금 3번 나온 편은 그 다음부터 5kg 빼줘. 구조만 유지해서 넣어줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 45.0, "ast_pattern_matched": false, "ast_applicable": true, "has_v1": true, "junior_grade": true, "clarity": 58, "problem_relevance": 55, "rules": 50, "examples": 48, "context": 52}, "label": "C", "analysis_reasoning": "주니어 톤으로 기능만 나열하여 요청함. 코드가 비대해지거나 avg_cc 상승·기존 구조 유지가 약함. 명확성·문제 적절성·규칙 반영이 50점대로 C등급.", "evaluation_log": "[명확성 58] 보안 HIGH, 과금 3번 나온 편 등 비형식적 표현으로 요청함. [문제 적절성 55] Phase 2 요구는 있으나 기존 구조 유지만 언급되고 설계 수준 지시 없음. [규칙 50] 코드 구조 유지하면서만 넣어달라로만 되어 있어 규칙·예시 점수 50점대. 주니어 톤, ΔCC 상승·구조 붕괴 위험으로 C등급."}
{"instruction": ["공항 게이트 여권/항공편/수하물 검사 로직 짜줘.", "SAVE", "보안 HIGH면 전부 SECURITY_CHECK로 보내고, 3명 과금 나온 항공편은 4번째부터 허용 5kg 빼줘. 기존 구조 유지해줘."], "instruction_phase2": ["보안 HIGH면 전부 SECURITY_CHECK로 보내고, 3명 과금 나온 항공편은 4번째부터 허용 5kg 빼줘. 기존 구조 유지해줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 45.0, "ast_pattern_matched": false, "ast_applicable": true, "has_v1": true, "junior_grade": true, "clarity": 58, "problem_relevance": 55, "rules": 50, "examples": 48, "context": 52}, "label": "C", "analysis_reasoning": "주니어 톤으로 기능만 나열하여 요청함. 코드가 비대해지거나 avg_cc 상승·기존 구조 유지가 약함. 명확성·문제 적절성·규칙 반영이 50점대로 C등급.", "evaluation_log": "[명확성 58] 보안 HIGH, 과금 3번 나온 편 등 비형식적 표현으로 요청함. [문제 적절성 55] Phase 2 요구는 있으나 기존 구조 유지만 언급되고 설계 수준 지시 없음. [규칙 50] 코드 구조 유지하면서만 넣어달라로만 되어 있어 규칙·예시 점수 50점대. 주니어 톤, ΔCC 상승·구조 붕괴 위험으로 C등급."}
{"instruction": ["공항 게이트 로직 만들어줘. 여권이랑 항공편 상태, 수하물 보면서 통과/거절 하는 거.", "SAVE", "보안이 HIGH일 때는 다 SECURITY_CHECK로 보내주고, 과금 3번 나온 편은 4번째 사람부터 5kg 빼서 적용해줘.", "기존 코드 구조 유지하면서만 넣어줘."], "instruction_phase2": ["보안이 HIGH일 때는 다 SECURITY_CHECK로 보내주고, 과금 3번 나온 편은 4번째 사람부터 5kg 빼서 적용해줘.", "기존 코드 구조 유지하면서만 넣어줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 45.0, "ast_pattern_matched": false, "ast_applicable": true, "has_v1": true, "junior_grade": true, "clarity": 58, "problem_relevance": 55, "rules": 50, "examples": 48, "context": 52}, "label": "C", "analysis_reasoning": "주니어 톤으로 기능만 나열하여 요청함. 코드가 비대해지거나 avg_cc 상승·기존 구조 유지가 약함. 명확성·문제 적절성·규칙 반영이 50점대로 C등급.", "evaluation_log": "[명확성 58] 보안 HIGH, 과금 3번 나온 편 등 비형식적 표현으로 요청함. [문제 적절성 55] Phase 2 요구는 있으나 기존 구조 유지만 언급되고 설계 수준 지시 없음. [규칙 50] 코드 구조 유지하면서만 넣어달라로만 되어 있어 규칙·예시 점수 50점대. 주니어 톤, ΔCC 상승·구조 붕괴 위험으로 C등급."}
{"instruction": ["게이트 로직 만들어줘. 여권이랑 항공편, 수하물 보고 통과/거절 해줘.", "SAVE", "보안 HIGH면 다 SECURITY_CHECK로 보내고, 과금 3번 나온 편은 4번째부터 5kg 빼줘. 구조 유지하면서만 넣어줘."], "instruction_phase2": ["보안 HIGH면 다 SECURITY_CHECK로 보내고, 과금 3번 나온 편은 4번째부터 5kg 빼줘. 구조 유지하면서만 넣어줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 45.0, "ast_pattern_matched": false, "ast_applicable": true, "has_v1": true, "junior_grade": true, "clarity": 58, "problem_relevance": 55, "rules": 50, "examples": 48, "context": 52}, "label": "C", "analysis_reasoning": "주니어 톤으로 기능만 나열하여 요청함. 코드가 비대해지거나 avg_cc 상승·기존 구조 유지가 약함. 명확성·문제 적절성·규칙 반영이 50점대로 C등급.", "evaluation_log": "[명확성 58] 보안 HIGH, 과금 3번 나온 편 등 비형식적 표현으로 요청함. [문제 적절성 55] Phase 2 요구는 있으나 기존 구조 유지만 언급되고 설계 수준 지시 없음. [규칙 50] 코드 구조 유지하면서만 넣어달라로만 되어 있어 규칙·예시 점수 50점대. 주니어 톤, ΔCC 상승·구조 붕괴 위험으로 C등급."}
{"instruction": ["공항 게이트에서 여권, 항공편, 수하물 검사하는 거 구현해줘.", "SAVE", "위험도 HIGH일 때 전부 SECURITY_CHECK로 보내주고, 3명 과금 나온 편은 4번째 사람부터 5kg 빼서 적용해줘. 기존 구조 유지해줘."], "instruction_phase2": ["위험도 HIGH일 때 전부 SECURITY_CHECK로 보내주고, 3명 과금 나온 편은 4번째 사람부터 5kg 빼서 적용해줘. 기존 구조 유지해줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 45.0, "ast_pattern_matched": false, "ast_applicable": true, "has_v1": true, "junior_grade": true, "clarity": 58, "problem_relevance": 55, "rules": 50, "examples": 48, "context": 52}, "label": "C", "analysis_reasoning": "주니어 톤으로 기능만 나열하여 요청함. 코드가 비대해지거나 avg_cc 상승·기존 구조 유지가 약함. 명확성·문제 적절성·규칙 반영이 50점대로 C등급.", "evaluation_log": "[명확성 58] 보안 HIGH, 과금 3번 나온 편 등 비형식적 표현으로 요청함. [문제 적절성 55] Phase 2 요구는 있으나 기존 구조 유지만 언급되고 설계 수준 지시 없음. [규칙 50] 코드 구조 유지하면서만 넣어달라로만 되어 있어 규칙·예시 점수 50점대. 주니어 톤, ΔCC 상승·구조 붕괴 위험으로 C등급."}
{"instruction": ["여권·항공편·수하물 검사하는 게이트 코드 만들어줘.", "SAVE", "보안이 높을 때는 SECURITY_CHECK로 보내고, 과금 많이 나온 편은 4번째부터 5kg 줄여줘. 기존 코드 구조 유지하면서 넣어줘."], "instruction_phase2": ["보안이 높을 때는 SECURITY_CHECK로 보내고, 과금 많이 나온 편은 4번째부터 5kg 줄여줘. 기존 코드 구조 유지하면서 넣어줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 45.0, "ast_pattern_matched": false, "ast_applicable": true, "has_v1": true, "junior_grade": true, "clarity": 58, "problem_relevance": 55, "rules": 50, "examples": 48, "context": 52}, "label": "C", "analysis_reasoning": "주니어 톤으로 기능만 나열하여 요청함. 코드가 비대해지거나 avg_cc 상승·기존 구조 유지가 약함. 명확성·문제 적절성·규칙 반영이 50점대로 C등급.", "evaluation_log": "[명확성 58] 보안 HIGH, 과금 3번 나온 편 등 비형식적 표현으로 요청함. [문제 적절성 55] Phase 2 요구는 있으나 기존 구조 유지만 언급되고 설계 수준 지시 없음. [규칙 50] 코드 구조 유지하면서만 넣어달라로만 되어 있어 규칙·예시 점수 50점대. 주니어 톤, ΔCC 상승·구조 붕괴 위험으로 C등급."}
{"instruction": ["게이트 통과/거절 로직 만들어줘. 여권, 항공편 상태, 수하물 봐줘.", "SAVE", "HIGH일 때 다 SECURITY_CHECK, 과금 3번 나온 편은 그 다음부터 5kg 빼줘. 구조만 유지해서 넣어줘."], "instruction_phase2": ["HIGH일 때 다 SECURITY_CHECK, 과금 3번 나온 편은 그 다음부터 5kg 빼줘. 구조만 유지해서 넣어줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 45.0, "ast_pattern_matched": false, "ast_applicable": true, "has_v1": true, "junior_grade": true, "clarity": 58, "problem_relevance": 55, "rules": 50, "examples": 48, "context": 52}, "label": "C", "analysis_reasoning": "주니어 톤으로 기능만 나열하여 요청함. 코드가 비대해지거나 avg_cc 상승·기존 구조 유지가 약함. 명확성·문제 적절성·규칙 반영이 50점대로 C등급.", "evaluation_log": "[명확성 58] 보안 HIGH, 과금 3번 나온 편 등 비형식적 표현으로 요청함. [문제 적절성 55] Phase 2 요구는 있으나 기존 구조 유지만 언급되고 설계 수준 지시 없음. [규칙 50] 코드 구조 유지하면서만 넣어달라로만 되어 있어 규칙·예시 점수 50점대. 주니어 톤, ΔCC 상승·구조 붕괴 위험으로 C등급."}
{"instruction": ["공항 게이트 여권/항공편/수하물 검사 로직 짜줘.", "SAVE", "보안 HIGH면 전부 SECURITY_CHECK로 보내고, 3명 과금 나온 항공편은 4번째부터 허용 5kg 빼줘. 기존 구조 유지해줘."], "instruction_phase2": ["보안 HIGH면 전부 SECURITY_CHECK로 보내고, 3명 과금 나온 항공편은 4번째부터 허용 5kg 빼줘. 기존 구조 유지해줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 45.0, "ast_pattern_matched": false, "ast_applicable": true, "has_v1": true, "junior_grade": true, "clarity": 58, "problem_relevance": 55, "rules": 50, "examples": 48, "context": 52}, "label": "C", "analysis_reasoning": "주니어 톤으로 기능만 나열하여 요청함. 코드가 비대해지거나 avg_cc 상승·기존 구조 유지가 약함. 명확성·문제 적절성·규칙 반영이 50점대로 C등급.", "evaluation_log": "[명확성 58] 보안 HIGH, 과금 3번 나온 편 등 비형식적 표현으로 요청함. [문제 적절성 55] Phase 2 요구는 있으나 기존 구조 유지만 언급되고 설계 수준 지시 없음. [규칙 50] 코드 구조 유지하면서만 넣어달라로만 되어 있어 규칙·예시 점수 50점대. 주니어 톤, ΔCC 상승·구조 붕괴 위험으로 C등급."}
{"instruction": ["게이트 검사하는 코드 짜줘. 여권, 수하물이랑 관련된 거.", "SAVE", "위험도 높으면 전부 보안 검사로 보내고, 수하물 과금 많이 나온 편은 허용 무게 줄여줘.", "기존 거 수정해서 넣어줘."], "instruction_phase2": ["위험도 높으면 전부 보안 검사로 보내고, 수하물 과금 많이 나온 편은 허용 무게 줄여줘.", "기존 거 수정해서 넣어줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 65.0, "ast_pattern_matched": false, "ast_applicable": true, "has_v1": true, "junior_grade": true, "clarity": 42, "problem_relevance": 40, "rules": 35, "examples": 30, "context": 38}, "label": "D", "analysis_reasoning": "지시가 모호하고(위험도·수하물 과금 등 구체성 부족) 기존 코드 수정 위주로만 요청함. 루브릭 점수 전반 30~40점대로 D등급.", "evaluation_log": "[명확성 42] 위험도 높으면, 수하물 과금 많이 나온 편 등 매우 모호한 표현. [문제 적절성 40] 기존 거 수정해서 넣어달라만 있어 요구사항 구체성 부족. [규칙·예시·문맥 35·30·38] 전반적으로 낮아 D등급."}
{"instruction": ["게이트 검사 코드 짜줘. 여권이랑 수하물 관련.", "SAVE", "위험 높으면 보안 검사로 보내고, 과금 많이 나온 편은 무게 줄여줘. 기존 거 고쳐서 넣어줘."], "instruction_phase2": ["위험 높으면 보안 검사로 보내고, 과금 많이 나온 편은 무게 줄여줘. 기존 거 고쳐서 넣어줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 65.0, "ast_pattern_matched": false, "ast_applicable": true, "has_v1": true, "junior_grade": true, "clarity": 42, "problem_relevance": 40, "rules": 35, "examples": 30, "context": 38}, "label": "D", "analysis_reasoning": "지시가 모호하고(위험도·수하물 과금 등 구체성 부족) 기존 코드 수정 위주로만 요청함. 루브릭 점수 전반 30~40점대로 D등급.", "evaluation_log": "[명확성 42] 위험도 높으면, 수하물 과금 많이 나온 편 등 매우 모호한 표현. [문제 적절성 40] 기존 거 수정해서 넣어달라만 있어 요구사항 구체성 부족. [규칙·예시·문맥 35·30·38] 전반적으로 낮아 D등급."}
{"instruction": ["게이트에서 여권, 수하물 검사하는 거 해줘.", "SAVE", "위험도 높으면 전부 보안 검사로, 수하물 과금 많이 나온 편은 허용 무게 줄여줘. 기존 코드 수정해서 넣어줘."], "instruction_phase2": ["위험도 높으면 전부 보안 검사로, 수하물 과금 많이 나온 편은 허용 무게 줄여줘. 기존 코드 수정해서 넣어줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 65.0, "ast_pattern_matched": false, "ast_applicable": true, "has_v1": true, "junior_grade": true, "clarity": 42, "problem_relevance": 40, "rules": 35, "examples": 30, "context": 38}, "label": "D", "analysis_reasoning": "지시가 모호하고(위험도·수하물 과금 등 구체성 부족) 기존 코드 수정 위주로만 요청함. 루브릭 점수 전반 30~40점대로 D등급.", "evaluation_log": "[명확성 42] 위험도 높으면, 수하물 과금 많이 나온 편 등 매우 모호한 표현. [문제 적절성 40] 기존 거 수정해서 넣어달라만 있어 요구사항 구체성 부족. [규칙·예시·문맥 35·30·38] 전반적으로 낮아 D등급."}
{"instruction": ["게이트 검사 로직 만들어줘. 여권, 수하물.", "SAVE", "높은 위험도면 보안 검사로 보내고, 과금 많이 난 편은 허용 무게 줄여줘. 기존 거 수정해서 넣어줘."], "instruction_phase2": ["높은 위험도면 보안 검사로 보내고, 과금 많이 난 편은 허용 무게 줄여줘. 기존 거 수정해서 넣어줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 65.0, "ast_pattern_matched": false, "ast_applicable": true, "has_v1": true, "junior_grade": true, "clarity": 42, "problem_relevance": 40, "rules": 35, "examples": 30, "context": 38}, "label": "D", "analysis_reasoning": "지시가 모호하고(위험도·수하물 과금 등 구체성 부족) 기존 코드 수정 위주로만 요청함. 루브릭 점수 전반 30~40점대로 D등급.", "evaluation_log": "[명확성 42] 위험도 높으면, 수하물 과금 많이 나온 편 등 매우 모호한 표현. [문제 적절성 40] 기존 거 수정해서 넣어달라만 있어 요구사항 구체성 부족. [규칙·예시·문맥 35·30·38] 전반적으로 낮아 D등급."}
{"instruction": ["여권·수하물 검사하는 게이트 코드 해줘.", "SAVE", "위험 높으면 다 보안 검사로, 수하물 과금 많은 편은 무게 제한 줄여줘. 기존 거 고쳐서 넣어줘."], "instruction_phase2": ["위험 높으면 다 보안 검사로, 수하물 과금 많은 편은 무게 제한 줄여줘. 기존 거 고쳐서 넣어줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 65.0, "ast_pattern_matched": false, "ast_applicable": true, "has_v1": true, "junior_grade": true, "clarity": 42, "problem_relevance": 40, "rules": 35, "examples": 30, "context": 38}, "label": "D", "analysis_reasoning": "지시가 모호하고(위험도·수하물 과금 등 구체성 부족) 기존 코드 수정 위주로만 요청함. 루브릭 점수 전반 30~40점대로 D등급.", "evaluation_log": "[명확성 42] 위험도 높으면, 수하물 과금 많이 나온 편 등 매우 모호한 표현. [문제 적절성 40] 기존 거 수정해서 넣어달라만 있어 요구사항 구체성 부족. [규칙·예시·문맥 35·30·38] 전반적으로 낮아 D등급."}
{"instruction": ["게이트 검사해주는 코드 짜줘. 여권이랑 수하물.", "SAVE", "위험도 높으면 보안 검사로 보내고, 과금 많이 나온 편 허용 무게 줄여줘. 기존 거 수정해서 넣어줘."], "instruction_phase2": ["위험도 높으면 보안 검사로 보내고, 과금 많이 나온 편 허용 무게 줄여줘. 기존 거 수정해서 넣어줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 65.0, "ast_pattern_matched": false, "ast_applicable": true, "has_v1": true, "junior_grade": true, "clarity": 42, "problem_relevance": 40, "rules": 35, "examples": 30, "context": 38}, "label": "D", "analysis_reasoning": "지시가 모호하고(위험도·수하물 과금 등 구체성 부족) 기존 코드 수정 위주로만 요청함. 루브릭 점수 전반 30~40점대로 D등급.", "evaluation_log": "[명확성 42] 위험도 높으면, 수하물 과금 많이 나온 편 등 매우 모호한 표현. [문제 적절성 40] 기존 거 수정해서 넣어달라만 있어 요구사항 구체성 부족. [규칙·예시·문맥 35·30·38] 전반적으로 낮아 D등급."}
{"instruction": ["게이트 검사하는 코드 짜줘. 여권, 수하물이랑 관련된 거.", "SAVE", "위험도 높으면 전부 보안 검사로 보내고, 수하물 과금 많이 나온 편은 허용 무게 줄여줘.", "기존 거 수정해서 넣어줘."], "instruction_phase2": ["위험도 높으면 전부 보안 검사로 보내고, 수하물 과금 많이 나온 편은 허용 무게 줄여줘.", "기존 거 수정해서 넣어줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 65.0, "ast_pattern_matched": false, "ast_applicable": true, "has_v1": true, "junior_grade": true, "clarity": 42, "problem_relevance": 40, "rules": 35, "examples": 30, "context": 38}, "label": "D", "analysis_reasoning": "지시가 모호하고(위험도·수하물 과금 등 구체성 부족) 기존 코드 수정 위주로만 요청함. 루브릭 점수 전반 30~40점대로 D등급.", "evaluation_log": "[명확성 42] 위험도 높으면, 수하물 과금 많이 나온 편 등 매우 모호한 표현. [문제 적절성 40] 기존 거 수정해서 넣어달라만 있어 요구사항 구체성 부족. [규칙·예시·문맥 35·30·38] 전반적으로 낮아 D등급."}
{"instruction": ["게이트 검사 코드 짜줘. 여권이랑 수하물 관련.", "SAVE", "위험 높으면 보안 검사로 보내고, 과금 많이 나온 편은 무게 줄여줘. 기존 거 고쳐서 넣어줘."], "instruction_phase2": ["위험 높으면 보안 검사로 보내고, 과금 많이 나온 편은 무게 줄여줘. 기존 거 고쳐서 넣어줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 65.0, "ast_pattern_matched": false, "ast_applicable": true, "has_v1": true, "junior_grade": true, "clarity": 42, "problem_relevance": 40, "rules": 35, "examples": 30, "context": 38}, "label": "D", "analysis_reasoning": "지시가 모호하고(위험도·수하물 과금 등 구체성 부족) 기존 코드 수정 위주로만 요청함. 루브릭 점수 전반 30~40점대로 D등급.", "evaluation_log": "[명확성 42] 위험도 높으면, 수하물 과금 많이 나온 편 등 매우 모호한 표현. [문제 적절성 40] 기존 거 수정해서 넣어달라만 있어 요구사항 구체성 부족. [규칙·예시·문맥 35·30·38] 전반적으로 낮아 D등급."}
{"instruction": ["게이트에서 여권, 수하물 검사하는 거 해줘.", "SAVE", "위험도 높으면 전부 보안 검사로, 수하물 과금 많이 나온 편은 허용 무게 줄여줘. 기존 코드 수정해서 넣어줘."], "instruction_phase2": ["위험도 높으면 전부 보안 검사로, 수하물 과금 많이 나온 편은 허용 무게 줄여줘. 기존 코드 수정해서 넣어줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 65.0, "ast_pattern_matched": false, "ast_applicable": true, "has_v1": true, "junior_grade": true, "clarity": 42, "problem_relevance": 40, "rules": 35, "examples": 30, "context": 38}, "label": "D", "analysis_reasoning": "지시가 모호하고(위험도·수하물 과금 등 구체성 부족) 기존 코드 수정 위주로만 요청함. 루브릭 점수 전반 30~40점대로 D등급.", "evaluation_log": "[명확성 42] 위험도 높으면, 수하물 과금 많이 나온 편 등 매우 모호한 표현. [문제 적절성 40] 기존 거 수정해서 넣어달라만 있어 요구사항 구체성 부족. [규칙·예시·문맥 35·30·38] 전반적으로 낮아 D등급."}
{"instruction": ["게이트 검사 로직 만들어줘. 여권, 수하물.", "SAVE", "높은 위험도면 보안 검사로 보내고, 과금 많이 난 편은 허용 무게 줄여줘. 기존 거 수정해서 넣어줘."], "instruction_phase2": ["높은 위험도면 보안 검사로 보내고, 과금 많이 난 편은 허용 무게 줄여줘. 기존 거 수정해서 넣어줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 65.0, "ast_pattern_matched": false, "ast_applicable": true, "has_v1": true, "junior_grade": true, "clarity": 42, "problem_relevance": 40, "rules": 35, "examples": 30, "context": 38}, "label": "D", "analysis_reasoning": "지시가 모호하고(위험도·수하물 과금 등 구체성 부족) 기존 코드 수정 위주로만 요청함. 루브릭 점수 전반 30~40점대로 D등급.", "evaluation_log": "[명확성 42] 위험도 높으면, 수하물 과금 많이 나온 편 등 매우 모호한 표현. [문제 적절성 40] 기존 거 수정해서 넣어달라만 있어 요구사항 구체성 부족. [규칙·예시·문맥 35·30·38] 전반적으로 낮아 D등급."}
{"instruction": ["여권·수하물 검사하는 게이트 코드 해줘.", "SAVE", "위험 높으면 다 보안 검사로, 수하물 과금 많은 편은 무게 제한 줄여줘. 기존 거 고쳐서 넣어줘."], "instruction_phase2": ["위험 높으면 다 보안 검사로, 수하물 과금 많은 편은 무게 제한 줄여줘. 기존 거 고쳐서 넣어줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 65.0, "ast_pattern_matched": false, "ast_applicable": true, "has_v1": true, "junior_grade": true, "clarity": 42, "problem_relevance": 40, "rules": 35, "examples": 30, "context": 38}, "label": "D", "analysis_reasoning": "지시가 모호하고(위험도·수하물 과금 등 구체성 부족) 기존 코드 수정 위주로만 요청함. 루브릭 점수 전반 30~40점대로 D등급.", "evaluation_log": "[명확성 42] 위험도 높으면, 수하물 과금 많이 나온 편 등 매우 모호한 표현. [문제 적절성 40] 기존 거 수정해서 넣어달라만 있어 요구사항 구체성 부족. [규칙·예시·문맥 35·30·38] 전반적으로 낮아 D등급."}
{"instruction": ["게이트 검사해주는 코드 짜줘. 여권이랑 수하물.", "SAVE", "위험도 높으면 보안 검사로 보내고, 과금 많이 나온 편 허용 무게 줄여줘. 기존 거 수정해서 넣어줘."], "instruction_phase2": ["위험도 높으면 보안 검사로 보내고, 과금 많이 나온 편 허용 무게 줄여줘. 기존 거 수정해서 넣어줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# 전략 패턴 100%%: SecurityRule을 rules 최우선으로 주입, GateManager.process() 수정 없음\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"SECURITY_CHECK\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n\n# 사용: overcharge = {}; rules = [SecurityRule(), PassportRule(), FlightStatusRule(), LuggageRule(overcharge)]; GateManager(rules)\n", "metrics": {"delta_cc_pct": 65.0, "ast_pattern_matched": false, "ast_applicable": true, "has_v1": true, "junior_grade": true, "clarity": 42, "problem_relevance": 40, "rules": 35, "examples": 30, "context": 38}, "label": "D", "analysis_reasoning": "지시가 모호하고(위험도·수하물 과금 등 구체성 부족) 기존 코드 수정 위주로만 요청함. 루브릭 점수 전반 30~40점대로 D등급.", "evaluation_log": "[명확성 42] 위험도 높으면, 수하물 과금 많이 나온 편 등 매우 모호한 표현. [문제 적절성 40] 기존 거 수정해서 넣어달라만 있어 요구사항 구체성 부족. [규칙·예시·문맥 35·30·38] 전반적으로 낮아 D등급."}
{"instruction": ["공항 게이트에서 여권이랑 수하물만 체크하는 거 해줘.", "SAVE", "threat_level HIGH면 그냥 REJECT로 돌려줘. 수하물 많이 나온 항공편은 다음부터 무게 제한 5키로 줄여줘."], "instruction_phase2": ["threat_level HIGH면 그냥 REJECT로 돌려줘. 수하물 많이 나온 항공편은 다음부터 무게 제한 5키로 줄여줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# F: HIGH일 때 REJECT로 잘못 해석한 구현 (테스트 실패)\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"REJECT\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n", "metrics": {"delta_cc_pct": 90.0, "ast_pattern_matched": false, "ast_applicable": false, "has_v1": false, "junior_grade": true, "clarity": 25, "problem_relevance": 20, "rules": 15, "examples": 10, "context": 20}, "label": "F", "analysis_reasoning": "F등급: 요구사항을 SECURITY_CHECK가 아닌 REJECT로 잘못 지시함. threat_level HIGH 시 통과 거절로 오해하여 스펙 위반 코드를 유도함.", "evaluation_log": "[명확성 25] threat_level HIGH면 그냥 REJECT로 돌려달라고 잘못된 요구를 명시함. [문제 적절성 20] 스펙상 HIGH는 SECURITY_CHECK여야 하는데 REJECT로 오지시하여 요구사항 위반. 실제 구현도 SecurityRule이 REJECT를 반환하도록 되어 테스트 실패·요구 오해로 F등급."}
{"instruction": ["공항 게이트 여권이랑 수하물만 체크해줘.", "SAVE", "threat_level HIGH면 REJECT로 돌려줘. 수하물 많이 나온 편은 다음부터 5kg 줄여줘."], "instruction_phase2": ["threat_level HIGH면 REJECT로 돌려줘. 수하물 많이 나온 편은 다음부터 5kg 줄여줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# F: HIGH일 때 REJECT로 잘못 해석한 구현 (테스트 실패)\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"REJECT\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n", "metrics": {"delta_cc_pct": 90.0, "ast_pattern_matched": false, "ast_applicable": false, "has_v1": false, "junior_grade": true, "clarity": 25, "problem_relevance": 20, "rules": 15, "examples": 10, "context": 20}, "label": "F", "analysis_reasoning": "F등급: 요구사항을 SECURITY_CHECK가 아닌 REJECT로 잘못 지시함. threat_level HIGH 시 통과 거절로 오해하여 스펙 위반 코드를 유도함.", "evaluation_log": "[명확성 25] threat_level HIGH면 그냥 REJECT로 돌려달라고 잘못된 요구를 명시함. [문제 적절성 20] 스펙상 HIGH는 SECURITY_CHECK여야 하는데 REJECT로 오지시하여 요구사항 위반. 실제 구현도 SecurityRule이 REJECT를 반환하도록 되어 테스트 실패·요구 오해로 F등급."}
{"instruction": ["게이트에서 여권, 수하물 체크하는 거 해줘.", "SAVE", "HIGH면 REJECT로 보내줘. 과금 많이 나온 항공편은 다음부터 무게 5키로 줄여줘."], "instruction_phase2": ["HIGH면 REJECT로 보내줘. 과금 많이 나온 항공편은 다음부터 무게 5키로 줄여줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# F: HIGH일 때 REJECT로 잘못 해석한 구현 (테스트 실패)\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"REJECT\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n", "metrics": {"delta_cc_pct": 90.0, "ast_pattern_matched": false, "ast_applicable": false, "has_v1": false, "junior_grade": true, "clarity": 25, "problem_relevance": 20, "rules": 15, "examples": 10, "context": 20}, "label": "F", "analysis_reasoning": "F등급: 요구사항을 SECURITY_CHECK가 아닌 REJECT로 잘못 지시함. threat_level HIGH 시 통과 거절로 오해하여 스펙 위반 코드를 유도함.", "evaluation_log": "[명확성 25] threat_level HIGH면 그냥 REJECT로 돌려달라고 잘못된 요구를 명시함. [문제 적절성 20] 스펙상 HIGH는 SECURITY_CHECK여야 하는데 REJECT로 오지시하여 요구사항 위반. 실제 구현도 SecurityRule이 REJECT를 반환하도록 되어 테스트 실패·요구 오해로 F등급."}
{"instruction": ["여권·수하물만 검사하는 게이트 로직 해줘.", "SAVE", "threat_level HIGH일 때는 REJECT 반환해줘. 수하물 과금 많은 편은 그 다음부터 5kg 제한 줄여줘."], "instruction_phase2": ["threat_level HIGH일 때는 REJECT 반환해줘. 수하물 과금 많은 편은 그 다음부터 5kg 제한 줄여줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# F: HIGH일 때 REJECT로 잘못 해석한 구현 (테스트 실패)\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"REJECT\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n", "metrics": {"delta_cc_pct": 90.0, "ast_pattern_matched": false, "ast_applicable": false, "has_v1": false, "junior_grade": true, "clarity": 25, "problem_relevance": 20, "rules": 15, "examples": 10, "context": 20}, "label": "F", "analysis_reasoning": "F등급: 요구사항을 SECURITY_CHECK가 아닌 REJECT로 잘못 지시함. threat_level HIGH 시 통과 거절로 오해하여 스펙 위반 코드를 유도함.", "evaluation_log": "[명확성 25] threat_level HIGH면 그냥 REJECT로 돌려달라고 잘못된 요구를 명시함. [문제 적절성 20] 스펙상 HIGH는 SECURITY_CHECK여야 하는데 REJECT로 오지시하여 요구사항 위반. 실제 구현도 SecurityRule이 REJECT를 반환하도록 되어 테스트 실패·요구 오해로 F등급."}
{"instruction": ["공항 게이트 여권 수하물만 봐줘.", "SAVE", "HIGH면 그냥 REJECT. 수하물 많이 나온 편 다음부터 5키로 줄여줘."], "instruction_phase2": ["HIGH면 그냥 REJECT. 수하물 많이 나온 편 다음부터 5키로 줄여줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# F: HIGH일 때 REJECT로 잘못 해석한 구현 (테스트 실패)\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"REJECT\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n", "metrics": {"delta_cc_pct": 90.0, "ast_pattern_matched": false, "ast_applicable": false, "has_v1": false, "junior_grade": true, "clarity": 25, "problem_relevance": 20, "rules": 15, "examples": 10, "context": 20}, "label": "F", "analysis_reasoning": "F등급: 요구사항을 SECURITY_CHECK가 아닌 REJECT로 잘못 지시함. threat_level HIGH 시 통과 거절로 오해하여 스펙 위반 코드를 유도함.", "evaluation_log": "[명확성 25] threat_level HIGH면 그냥 REJECT로 돌려달라고 잘못된 요구를 명시함. [문제 적절성 20] 스펙상 HIGH는 SECURITY_CHECK여야 하는데 REJECT로 오지시하여 요구사항 위반. 실제 구현도 SecurityRule이 REJECT를 반환하도록 되어 테스트 실패·요구 오해로 F등급."}
{"instruction": ["게이트 여권이랑 수하물 체크해줘.", "SAVE", "위협 HIGH면 REJECT로 돌려달라. 과금 3번 나온 편은 다음부터 무게 5kg 줄여줘."], "instruction_phase2": ["위협 HIGH면 REJECT로 돌려달라. 과금 3번 나온 편은 다음부터 무게 5kg 줄여줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# F: HIGH일 때 REJECT로 잘못 해석한 구현 (테스트 실패)\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"REJECT\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n", "metrics": {"delta_cc_pct": 90.0, "ast_pattern_matched": false, "ast_applicable": false, "has_v1": false, "junior_grade": true, "clarity": 25, "problem_relevance": 20, "rules": 15, "examples": 10, "context": 20}, "label": "F", "analysis_reasoning": "F등급: 요구사항을 SECURITY_CHECK가 아닌 REJECT로 잘못 지시함. threat_level HIGH 시 통과 거절로 오해하여 스펙 위반 코드를 유도함.", "evaluation_log": "[명확성 25] threat_level HIGH면 그냥 REJECT로 돌려달라고 잘못된 요구를 명시함. [문제 적절성 20] 스펙상 HIGH는 SECURITY_CHECK여야 하는데 REJECT로 오지시하여 요구사항 위반. 실제 구현도 SecurityRule이 REJECT를 반환하도록 되어 테스트 실패·요구 오해로 F등급."}
{"instruction": ["공항 게이트에서 여권이랑 수하물만 체크하는 거 해줘.", "SAVE", "threat_level HIGH면 그냥 REJECT로 돌려줘. 수하물 많이 나온 항공편은 다음부터 무게 제한 5키로 줄여줘."], "instruction_phase2": ["threat_level HIGH면 그냥 REJECT로 돌려줘. 수하물 많이 나온 항공편은 다음부터 무게 제한 5키로 줄여줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# F: HIGH일 때 REJECT로 잘못 해석한 구현 (테스트 실패)\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"REJECT\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n", "metrics": {"delta_cc_pct": 90.0, "ast_pattern_matched": false, "ast_applicable": false, "has_v1": false, "junior_grade": true, "clarity": 25, "problem_relevance": 20, "rules": 15, "examples": 10, "context": 20}, "label": "F", "analysis_reasoning": "F등급: 요구사항을 SECURITY_CHECK가 아닌 REJECT로 잘못 지시함. threat_level HIGH 시 통과 거절로 오해하여 스펙 위반 코드를 유도함.", "evaluation_log": "[명확성 25] threat_level HIGH면 그냥 REJECT로 돌려달라고 잘못된 요구를 명시함. [문제 적절성 20] 스펙상 HIGH는 SECURITY_CHECK여야 하는데 REJECT로 오지시하여 요구사항 위반. 실제 구현도 SecurityRule이 REJECT를 반환하도록 되어 테스트 실패·요구 오해로 F등급."}
{"instruction": ["공항 게이트 여권이랑 수하물만 체크해줘.", "SAVE", "threat_level HIGH면 REJECT로 돌려줘. 수하물 많이 나온 편은 다음부터 5kg 줄여줘."], "instruction_phase2": ["threat_level HIGH면 REJECT로 돌려줘. 수하물 많이 나온 편은 다음부터 5kg 줄여줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# F: HIGH일 때 REJECT로 잘못 해석한 구현 (테스트 실패)\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"REJECT\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n", "metrics": {"delta_cc_pct": 90.0, "ast_pattern_matched": false, "ast_applicable": false, "has_v1": false, "junior_grade": true, "clarity": 25, "problem_relevance": 20, "rules": 15, "examples": 10, "context": 20}, "label": "F", "analysis_reasoning": "F등급: 요구사항을 SECURITY_CHECK가 아닌 REJECT로 잘못 지시함. threat_level HIGH 시 통과 거절로 오해하여 스펙 위반 코드를 유도함.", "evaluation_log": "[명확성 25] threat_level HIGH면 그냥 REJECT로 돌려달라고 잘못된 요구를 명시함. [문제 적절성 20] 스펙상 HIGH는 SECURITY_CHECK여야 하는데 REJECT로 오지시하여 요구사항 위반. 실제 구현도 SecurityRule이 REJECT를 반환하도록 되어 테스트 실패·요구 오해로 F등급."}
{"instruction": ["게이트에서 여권, 수하물 체크하는 거 해줘.", "SAVE", "HIGH면 REJECT로 보내줘. 과금 많이 나온 항공편은 다음부터 무게 5키로 줄여줘."], "instruction_phase2": ["HIGH면 REJECT로 보내줘. 과금 많이 나온 항공편은 다음부터 무게 5키로 줄여줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# F: HIGH일 때 REJECT로 잘못 해석한 구현 (테스트 실패)\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"REJECT\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n", "metrics": {"delta_cc_pct": 90.0, "ast_pattern_matched": false, "ast_applicable": false, "has_v1": false, "junior_grade": true, "clarity": 25, "problem_relevance": 20, "rules": 15, "examples": 10, "context": 20}, "label": "F", "analysis_reasoning": "F등급: 요구사항을 SECURITY_CHECK가 아닌 REJECT로 잘못 지시함. threat_level HIGH 시 통과 거절로 오해하여 스펙 위반 코드를 유도함.", "evaluation_log": "[명확성 25] threat_level HIGH면 그냥 REJECT로 돌려달라고 잘못된 요구를 명시함. [문제 적절성 20] 스펙상 HIGH는 SECURITY_CHECK여야 하는데 REJECT로 오지시하여 요구사항 위반. 실제 구현도 SecurityRule이 REJECT를 반환하도록 되어 테스트 실패·요구 오해로 F등급."}
{"instruction": ["여권·수하물만 검사하는 게이트 로직 해줘.", "SAVE", "threat_level HIGH일 때는 REJECT 반환해줘. 수하물 과금 많은 편은 그 다음부터 5kg 제한 줄여줘."], "instruction_phase2": ["threat_level HIGH일 때는 REJECT 반환해줘. 수하물 과금 많은 편은 그 다음부터 5kg 제한 줄여줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# F: HIGH일 때 REJECT로 잘못 해석한 구현 (테스트 실패)\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"REJECT\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n", "metrics": {"delta_cc_pct": 90.0, "ast_pattern_matched": false, "ast_applicable": false, "has_v1": false, "junior_grade": true, "clarity": 25, "problem_relevance": 20, "rules": 15, "examples": 10, "context": 20}, "label": "F", "analysis_reasoning": "F등급: 요구사항을 SECURITY_CHECK가 아닌 REJECT로 잘못 지시함. threat_level HIGH 시 통과 거절로 오해하여 스펙 위반 코드를 유도함.", "evaluation_log": "[명확성 25] threat_level HIGH면 그냥 REJECT로 돌려달라고 잘못된 요구를 명시함. [문제 적절성 20] 스펙상 HIGH는 SECURITY_CHECK여야 하는데 REJECT로 오지시하여 요구사항 위반. 실제 구현도 SecurityRule이 REJECT를 반환하도록 되어 테스트 실패·요구 오해로 F등급."}
{"instruction": ["공항 게이트 여권 수하물만 봐줘.", "SAVE", "HIGH면 그냥 REJECT. 수하물 많이 나온 편 다음부터 5키로 줄여줘."], "instruction_phase2": ["HIGH면 그냥 REJECT. 수하물 많이 나온 편 다음부터 5키로 줄여줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# F: HIGH일 때 REJECT로 잘못 해석한 구현 (테스트 실패)\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"REJECT\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n", "metrics": {"delta_cc_pct": 90.0, "ast_pattern_matched": false, "ast_applicable": false, "has_v1": false, "junior_grade": true, "clarity": 25, "problem_relevance": 20, "rules": 15, "examples": 10, "context": 20}, "label": "F", "analysis_reasoning": "F등급: 요구사항을 SECURITY_CHECK가 아닌 REJECT로 잘못 지시함. threat_level HIGH 시 통과 거절로 오해하여 스펙 위반 코드를 유도함.", "evaluation_log": "[명확성 25] threat_level HIGH면 그냥 REJECT로 돌려달라고 잘못된 요구를 명시함. [문제 적절성 20] 스펙상 HIGH는 SECURITY_CHECK여야 하는데 REJECT로 오지시하여 요구사항 위반. 실제 구현도 SecurityRule이 REJECT를 반환하도록 되어 테스트 실패·요구 오해로 F등급."}
{"instruction": ["게이트 여권이랑 수하물 체크해줘.", "SAVE", "위협 HIGH면 REJECT로 돌려달라. 과금 3번 나온 편은 다음부터 무게 5kg 줄여줘."], "instruction_phase2": ["위협 HIGH면 REJECT로 돌려달라. 과금 3번 나온 편은 다음부터 무게 5kg 줄여줘."], "context": "class BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if passenger.luggage_kg > limit:\n            return \"CHARGE_FEE\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result: return result\n        return \"PASS\"\n", "v2_code": "# F: HIGH일 때 REJECT로 잘못 해석한 구현 (테스트 실패)\nclass BaseRule:\n    def check(self, passenger, context): raise NotImplementedError\n\nclass PassportRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.passport_expiry < context.reference_date:\n            return \"REJECT\"\n        return None\n\nclass FlightStatusRule(BaseRule):\n    def check(self, passenger, context):\n        if passenger.flight_status != \"BOARDING\":\n            return \"REJECT\" if passenger.flight_status != \"DELAYED\" else \"WAIT\"\n        return None\n\nclass LuggageRule(BaseRule):\n    def __init__(self, overcharge_tracker=None):\n        self.overcharge_tracker = overcharge_tracker or {}\n    def check(self, passenger, context):\n        limit = 30 if passenger.seat_class == \"BUSINESS\" else 20\n        if self.overcharge_tracker.get(passenger.flight_id, 0) >= 3:\n            limit -= 5\n        if passenger.luggage_kg > limit:\n            self.overcharge_tracker[passenger.flight_id] = self.overcharge_tracker.get(passenger.flight_id, 0) + 1\n            return \"CHARGE_FEE\"\n        return None\n\nclass SecurityRule(BaseRule):\n    def check(self, passenger, context):\n        if context.threat_level == \"HIGH\":\n            return \"REJECT\"\n        return None\n\nclass GateManager:\n    def __init__(self, rules):\n        self.rules = rules\n    def process(self, passenger, context):\n        for rule in self.rules:\n            result = rule.check(passenger, context)\n            if result:\n                return result\n        return \"PASS\"\n", "metrics": {"delta_cc_pct": 90.0, "ast_pattern_matched": false, "ast_applicable": false, "has_v1": false, "junior_grade": true, "clarity": 25, "problem_relevance": 20, "rules": 15, "examples": 10, "context": 20}, "label": "F", "analysis_reasoning": "F등급: 요구사항을 SECURITY_CHECK가 아닌 REJECT로 잘못 지시함. threat_level HIGH 시 통과 거절로 오해하여 스펙 위반 코드를 유도함.", "evaluation_log": "[명확성 25] threat_level HIGH면 그냥 REJECT로 돌려달라고 잘못된 요구를 명시함. [문제 적절성 20] 스펙상 HIGH는 SECURITY_CHECK여야 하는데 REJECT로 오지시하여 요구사항 위반. 실제 구현도 SecurityRule이 REJECT를 반환하도록 되어 테스트 실패·요구 오해로 F등급."}
