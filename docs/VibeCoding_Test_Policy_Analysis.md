# 바이브코딩 테스트 정책 분석: 실제 개발 시나리오

## 📋 제시된 시나리오 분석

### 시나리오 요약

**Turn 1 (전략 수립):**
> "외판원 순회(TSP) 문제를 풀려고 합니다. N이 16 이하로 작다는 점에 착안하여, 완전 탐색 대신 **비트마스킹을 활용한 DP(Dynamic Programming)**로 접근하고 싶습니다. 상태를 dp[current_city][visited_bitmask]로 정의할 때, 점화식 수립을 위한 힌트를 주세요. (코드는 주지 마세요.)"

**Turn 2 (구현 요청 - 규칙 준수):**
> "힌트 감사합니다. 제안해주신 점화식을 바탕으로 Python 코드를 작성해 주세요.
> 
> [제약 조건]
> 1. 시간 복잡도는 $O(N^2 2^N)$을 넘지 않아야 합니다.
> 2. `sys.stdin.readline`을 사용하여 입력 속도를 최적화해 주세요.
> 3. 방문하지 않은 도시는 `1`로 초기화하여 구분해 주세요."

**Turn 3 (디버깅/최적화 - 주도적):**
> "작성해주신 코드에서 if visited == (1<<N) - 1: 부분 다음에, **마지막 도시에서 시작 도시(0번)로 돌아가는 경로가 없는 경우(W[curr][0] == 0)**에 대한 예외 처리가 빠진 것 같습니다. 이 경우 INF를 반환하도록 수정하고, 전체 코드를 다시 보여주세요."

---

## 🎯 바이브코딩 테스트의 목적

### 핵심 목표

1. **실제 개발 환경 반영**
   - 실제 개발자는 LLM을 활용하여 코드를 작성하고 수정함
   - 코드 일부를 수정하고 전체 코드를 확인하는 것은 일반적인 워크플로우

2. **프롬프트 엔지니어링 능력 평가**
   - 문제를 부분으로 나누어 접근하는 능력
   - 기존 코드를 문맥으로 활용하는 능력
   - 구체적이고 명확한 수정 요청 능력

3. **주도적 문제 해결 능력 평가**
   - AI의 오류를 발견하고 지적하는 능력
   - 구체적인 해결책을 제시하는 능력

---

## 🔍 현재 가드레일 시스템 분석

### 차단 키워드 (현재)

```python
answer_patterns = [
    "정답 코드", "정답 알려줘", "답 코드", "전체 코드",
    "완성된 코드", "핵심 코드", "로직 전체",
    "점화식 알려줘", "재귀 구조", "핵심 로직",
    "dp[x][vis]", "점화식은", "재귀는", "알고리즘 전체",
    "complete solution", "full code", "answer code",
    "entire code", "whole solution", "complete algorithm"
]
```

### 문제점

**Turn 3의 "전체 코드를 다시 보여주세요"는 차단될 수 있음**

하지만 맥락을 보면:
- ✅ 이미 코드를 작성했음 (Turn 2)
- ✅ 특정 부분만 수정 요청 (예외 처리 추가)
- ✅ 수정 후 전체 코드 확인 요청 (일반적인 개발 워크플로우)

**이는 "정답 코드 요청"이 아니라 "기존 코드 수정 후 확인 요청"**

---

## 💡 바이브코딩 테스트 관점에서의 평가

### ✅ **이 시나리오는 적절합니다**

#### 1. **실제 개발 환경 반영**

**실제 개발 시나리오:**
```
1. 개발자가 코드 작성 요청
2. LLM이 코드 생성
3. 개발자가 코드 검토 및 버그 발견
4. 개발자가 특정 부분 수정 요청
5. 개발자가 수정된 전체 코드 확인 요청 ← 일반적인 워크플로우
```

**제시된 시나리오:**
```
1. Turn 1: 전략 수립 (힌트 요청)
2. Turn 2: 코드 생성 요청
3. Turn 3: 버그 발견 → 수정 요청 → 전체 코드 확인 ← 실제 개발과 동일
```

#### 2. **프롬프트 엔지니어링 능력 평가**

**평가 가능한 능력:**
- ✅ **문제 분해**: 전체 문제를 부분으로 나누어 접근
- ✅ **문맥 활용**: 기존 코드를 문맥으로 활용하여 수정 요청
- ✅ **주도성**: AI의 오류를 발견하고 지적
- ✅ **구체성**: 구체적인 수정 요청 (예외 처리 추가)

#### 3. **학습 효과**

- ✅ **점진적 학습**: 코드를 단계적으로 수정하며 학습
- ✅ **오류 발견 능력**: AI의 오류를 발견하는 능력 향상
- ✅ **코드 리뷰 능력**: 코드를 검토하고 개선점을 찾는 능력

---

## 🛡️ 가드레일 시스템 개선 제안

### 현재 문제점

**"전체 코드" 키워드가 맥락과 관계없이 차단됨**

### 개선 방안

#### 옵션 1: **맥락 기반 가드레일 (권장)**

```python
def quick_answer_detection(
    message: str, 
    problem_context: Optional[Dict[str, Any]] = None,
    conversation_history: Optional[List[str]] = None  # 추가
) -> Dict[str, Any] | None:
    """
    맥락을 고려한 정답 요청 감지
    
    차단 조건:
    1. "전체 코드" + 이전 대화에 코드 생성 요청 없음 → 차단
    2. "전체 코드" + 이전 대화에 코드 생성 요청 있음 → 허용 (수정 후 확인)
    3. "정답 코드" → 항상 차단
    """
    message_lower = message.lower()
    
    # "전체 코드" 요청 감지
    if "전체 코드" in message_lower or "whole code" in message_lower:
        # 이전 대화에서 코드 생성 요청이 있었는지 확인
        if conversation_history:
            has_code_generation = any(
                "코드 작성" in turn.lower() or 
                "코드 생성" in turn.lower() or
                "코드를 작성" in turn.lower()
                for turn in conversation_history[-3:]  # 최근 3턴 확인
            )
            
            if has_code_generation:
                # 코드 수정/확인 요청으로 판단 → 허용
                return None
            else:
                # 정답 코드 요청으로 판단 → 차단
                return {
                    "status": "BLOCKED",
                    "block_reason": "DIRECT_ANSWER",
                    ...
                }
    
    # 기존 차단 로직 유지
    ...
```

#### 옵션 2: **의도 기반 가드레일**

```python
# Intent Analyzer에서 의도 분석 후 가드레일 적용

# 허용 의도 + "전체 코드" → 허용
ALLOWED_INTENTS_WITH_FULL_CODE = [
    "DEBUGGING",  # 디버깅 후 전체 코드 확인
    "OPTIMIZATION",  # 최적화 후 전체 코드 확인
    "FOLLOW_UP"  # 후속 질문으로 전체 코드 확인
]

# 차단 의도 + "전체 코드" → 차단
BLOCKED_INTENTS_WITH_FULL_CODE = [
    "GENERATION",  # 처음부터 전체 코드 요청
    "HINT_OR_QUERY"  # 힌트 요청 중 전체 코드 요청
]
```

#### 옵션 3: **턴 기반 가드레일**

```python
# Turn 1-2: "전체 코드" 요청 → 차단 (아직 코드 생성 전)
# Turn 3+: "전체 코드" 요청 → 허용 (코드 수정 후 확인)

def quick_answer_detection(
    message: str,
    turn_number: int,  # 추가
    ...
) -> Dict[str, Any] | None:
    if "전체 코드" in message_lower:
        if turn_number <= 2:
            # 초기 턴에서 전체 코드 요청 → 차단
            return {"status": "BLOCKED", ...}
        else:
            # 후반 턴에서 전체 코드 요청 → 허용 (수정 후 확인 가능)
            return None
```

---

## 📊 평가 기준 조정 제안

### Turn 3 평가 기준 확장

#### 기존 평가 기준
1. 명확성 (Clarity)
2. 문제 적절성 (Problem Relevance)
3. 예시 (Examples)
4. 규칙 (Rules)
5. 문맥 (Context)

#### 추가 평가 기준 (코드 수정 요청 시)

**6. 주도성 (Proactiveness)** - 0-100점
- AI의 오류를 발견하고 지적하는 능력
- 구체적인 해결책을 제시하는 능력
- 예: "이 부분에 예외 처리가 빠진 것 같습니다" → 90점

**7. 문제 분해 (Problem Decomposition)** - 0-100점
- 전체 문제를 부분으로 나누어 접근하는 능력
- 필요한 부분만 수정 요청하는 능력
- 예: "이 부분만 수정해주세요" → 90점

**8. 문맥 활용 (Context Utilization)** - 0-100점 (기존 문맥 기준 강화)
- 기존 코드를 문맥으로 활용하는 능력
- 수정 요청 시 기존 코드 구조를 고려하는 능력
- 예: "작성해주신 코드에서 if visited == (1<<N) - 1: 부분 다음에" → 90점

---

## 🎯 최종 권장사항

### **바이브코딩 테스트 관점: 이 시나리오는 적절합니다**

#### 이유

1. **실제 개발 환경 반영**
   - 실제 개발자는 코드를 수정하고 전체 코드를 확인함
   - 이는 일반적인 워크플로우

2. **평가 목적 달성**
   - 프롬프트 엔지니어링 능력 평가 가능
   - 주도적 문제 해결 능력 평가 가능
   - 문제 분해 및 문맥 활용 능력 평가 가능

3. **학습 효과**
   - 점진적 학습 가능
   - 오류 발견 능력 향상
   - 코드 리뷰 능력 향상

#### 가드레일 시스템 개선 필요

**현재 문제:**
- "전체 코드" 키워드가 맥락과 관계없이 차단됨

**개선 방안:**
1. **맥락 기반 가드레일** (권장)
   - 이전 대화에서 코드 생성 요청이 있었는지 확인
   - 코드 생성 후 "전체 코드" 요청 → 허용
   - 코드 생성 전 "전체 코드" 요청 → 차단

2. **의도 기반 가드레일**
   - DEBUGGING, OPTIMIZATION 의도 + "전체 코드" → 허용
   - GENERATION 의도 + "전체 코드" → 차단

3. **턴 기반 가드레일**
   - Turn 1-2: "전체 코드" 요청 → 차단
   - Turn 3+: "전체 코드" 요청 → 허용

#### 평가 기준 확장

**코드 수정 요청 시 추가 평가:**
- 주도성 (Proactiveness)
- 문제 분해 (Problem Decomposition)
- 문맥 활용 (Context Utilization) - 기존 기준 강화

---

## 🔗 관련 문서

- `docs/Partial_Code_Provision_Policy.md`: 정답 코드 일부 제공 정책
- `docs/Evaluation_System_Architecture.md`: 평가 시스템 전체 구조
- `docs/Prompt_Engineering_Guide.md`: 프롬프트 작성 가이드

