# 프로덕션 환경 설정 예시
# 이 파일을 .env.prod로 복사하여 사용하세요
# 또는 환경 변수로 직접 설정

# 앱 설정
APP_NAME=AI Vibe Coding Test Worker
APP_VERSION=0.1.0
DEBUG=false

# FastAPI 설정
API_HOST=0.0.0.0
API_PORT=8000

# 배포 환경 PostgreSQL 설정
# 실제 배포 DB 정보로 변경하세요
POSTGRES_HOST=your-production-db-host.com
POSTGRES_PORT=5432
POSTGRES_USER=your_db_user
POSTGRES_PASSWORD=your_db_password
POSTGRES_DB=ai_vibe_coding_test

# Redis 설정 (Docker 컨테이너 또는 외부 Redis)
REDIS_HOST=redis  # Docker 네트워크 내부: redis, 외부 Redis: 실제 호스트
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# LLM API 설정
GEMINI_API_KEY=your_gemini_api_key_here

# LLM 모델 설정
DEFAULT_LLM_MODEL=gemini-2.5-flash
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=4096

# Judge0 설정
JUDGE0_API_URL=https://judge0-ce.p.rapidapi.com
JUDGE0_API_KEY=your_rapidapi_key_here
JUDGE0_USE_RAPIDAPI=true
JUDGE0_RAPIDAPI_HOST=judge0-ce.p.rapidapi.com

# Spring Boot 콜백 설정
SPRING_CALLBACK_URL=https://your-spring-backend.com/api/ai/callback
SPRING_API_KEY=your_spring_api_key

# 큐 시스템 설정
USE_REDIS_QUEUE=true

# LangGraph 체크포인트 설정
CHECKPOINT_TTL_SECONDS=3600


